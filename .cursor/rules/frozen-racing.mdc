---
description: 赛车品类冻结 — B7 验收基线，禁止无意修改
globs:
  - "templates/racing/**"
  - "templates/_edu/racing_hooks.gd"
  - "config/code_anchors/racing.json"
  - "config/creative_templates/racing.json"
  - "config/frozen_genre_racing.json"
  - "开发文档/模板引擎/快照/frozen/6.24_racing_v1.0/**"
alwaysApply: false
---

# 赛车（racing）· 冻结约束（2026-06-24 v1.0）

## 适用范围

编辑 **racing（欢乐赛车）** 相关路径时生效。

## 禁止（默认）

- **禁止**修改 `templates/racing/**`（含 `core/`、`config/`、`scenes/`）
- **禁止**修改 `templates/_edu/racing_hooks.gd`
- **禁止**修改 `config/code_anchors/racing.json`、`config/creative_templates/racing.json`
- **禁止**为修其它品类回改赛车锚点、hooks、B7 动作映射

## 共享依赖（慎改）

改动以下文件可能影响赛车 B7，须**显式解冻**并回归赛车验收：

- `templates/_edu/edu_action_bridge.gd`
- `kiosk/edu/code-highlight.js`
- `config/kiosk_edu_spec.json`（`code_highlight` 段）
- `kiosk/edu/edu-wizard.js` 内 **racing** 分支（`getGenreHighlightFallback` · B7 演示钮）

## 解冻流程

1. 用户确认解冻范围
2. 修改后执行：
   ```powershell
   python 05-工具脚本\freeze_racing_snapshot.py write
   ```
3. 回归：B7「撞车」「撞路障」· Godot 真机碰撞 · `freeze_racing_snapshot.py verify`

## 权威文档

- [`6.24_赛车_冻结快照_v1.0.md`](开发文档/模板引擎/快照/6.24_赛车_冻结快照_v1.0.md)
- 清单：`config/frozen_genre_racing.json`
