---
description: Godot K12 小游戏 AI 开发约束
globs: "**/*.{gd,tscn,tres,godot}"
alwaysApply: true
---

# AI 小游戏创作工坊 · Cursor 开发约束

- 使用 Godot 4.6 Standard + GDScript，**全部函数强类型**
- **当前主线**：AI 改游戏智能体（通用落地工作流）；非主线见 `开发文档/工作方向锁定_AI改游戏智能体_v1.md`
- 单次会话只改 **一个品类模板** 目录（改 `_edu`/契约/Agent 时除外）
- **禁止修改 `templates/*/core` 等玩法冻结源**；`templates/_edu/` 可改（桥/触控/注入）
- **会话智能体**：改 `workspace/{session}/`；**本轮原话优先**（对话体验对齐大模型）；catalog/桥是捷径；声称⊆磁盘；手动重开
- 需求：`开发文档/AI改游戏智能体需求_v1.md`（**v1.2**）· 根因：`…_根因分析与闭环改造_v1.md` · 施工：`…_施工规范手册_v1.md` · 映射：`…_文件映射表_v1.md`
- 开工提示：`开发文档/AI改游戏智能体_全任务开工提示词_v3.md`（v2 已归档）
- **回主页 / release**：先 harvest（验证质量优先）→ Learned Skill（`data/learned_skills/`），再销毁 workspace
- **LLM 连不上**：stub 降级，UI 诚实
- 试玩窗口：全屏 + Always on Top
- 禁止：联机、存档、内购、广告、商城；GDScript 禁 OS.execute / HTTP 等；禁止幻想 API / 只改 config 不开 runtime
- 场景深度 ≤ 5；单场景节点 ≤ 80；素材 `assets/` + Kenney CC0
- 完成后 **godot-mcp run_project** 无 ERROR；面向 14 岁以下；操作 ≤ 3 键
- 演示导出：Windows exe 优先
