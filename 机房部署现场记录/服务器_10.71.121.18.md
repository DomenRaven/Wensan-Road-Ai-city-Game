# 教室服务器 · 10.71.121.18

> **最后更新**：2026-07-21 午  
> **项目根**：`E:\project\GameForge-K12`  
> **学生入口**：http://10.71.121.18:8080/kiosk/edu/  
> **API**：http://10.71.121.18:8000  
> **workspace UNC**：`\\10.71.121.18\GameForgeWorkspace` → 学生映射 `Z:`

---

## 总状态

| 域 | 状态 |
|----|------|
| 基础部署（首装） | ✅ PASS |
| 手册 §1 防火墙 8000/8080 | ✅ |
| 手册 §2 SMB 共享 | ✅ |
| 手册 §3 SMB 445 | ✅ |
| 手册 §4 健康检查 | ✅ |
| 手册 §5 服务启动 | ✅ 已在跑（跳过） |
| 手册 §6 备份 | ⏸ 未记录 |
| 手册 §7 smoke + pytest | ✅ |
| 手册 §8–§9 学生侧 | ⏸ 待学生机 · `_tools` 助手已发布 ✅ |

**结论：服务器端可认为全面通过，等待学生机 §8/§9。**

---

## 配置快照（不含密钥）

| 项 | 值 |
|----|-----|
| `PLAY_LAUNCH_MODE` | `local_share` |
| `MAX_SESSIONS` | `70` |
| `MAX_CONCURRENT_AGENTS` | `6`（health/smoke 口径） |
| `GODOT_PATH` | `C:\Godot\Godot_v4.6.3-stable_win64.exe` |
| Godot 学生安装源（Z:） | `Z:\_tools\Godot\Godot_v4.6.3-stable_win64.exe` |
| 机房助手学生安装源（Z:） | `Z:\_tools\GameForgeLabHelper.exe` |
| 机房助手服务器磁盘 | `E:\project\GameForge-K12\workspace\_tools\GameForgeLabHelper.exe` |
| `LLM_API_KEY` | 已设置（勿写明文） |
| `config/kiosk_edu_spec.json` · `api_base` | `http://10.71.121.18:8000` |
| `session_backend` / Redis | `store_ok=True`（2026-07-21 午） |
| `active_sessions` | 9（午检时；随使用变化） |

---

## 网络与共享

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 防火墙 GameForge API 8000 | Enabled · Allow | |
| 防火墙 GameForge Kiosk 8080 | Enabled · Allow | |
| 文件和打印机共享入站 | Enabled · Allow | §3 |
| TCP 8000 Listen | `0.0.0.0` | |
| TCP 8080 Listen | `0.0.0.0` | |
| TCP 445 Listen | `::` | Windows 双栈正常 |
| SMB 共享名 | `GameForgeWorkspace` | |
| 共享路径 | `E:\project\GameForge-K12\workspace` | |
| UNC 本机可读 | ✅ | |

**路径换算：**

```text
E:\project\GameForge-K12\workspace\users\{uid}\{sid}
  →  Z:\users\{uid}\{sid}
```

**ACL**：共享权限 Users=Change；NTFS 逐用户隔离 **待 IT**（首课可内网共享简化）。

---

## 验收命令记录（2026-07-21）

### health（本机 + LAN）

```text
status=ok · store_ok=True · max_sessions=70 · play_launch_mode=local_share
Kiosk 127.0.0.1 + 10.71.121.18 → 200
```

### smoke_7_20_nb_p0.py

```text
PASS  NB-12 · max_sessions=70, agents=6
NOTE  NB-01 Playwright 跳过（无 playwright 模块）· 静态契约
PASS  NB-01 · NB-06 · NB-07 · NB-03 · NB-10
=== 全部通过 ===
```

### pytest 门禁簇

```text
28 passed in 0.35s
（test_hf15_1_hollow_done + test_hf15_oracle_layers + test_hf14_evidence）
```

---

## 运维备忘

| 场景 | 动作 |
|------|------|
| 服务器重启后 | Redis（可选）→ 手册 §5 起 API/Kiosk（`0.0.0.0`）→ §4 复验 |
| 勿用 | `launch_workshop.py` / `deploy_exhibition.ps1` 作机房常驻（默认 127.0.0.1） |
| 排障 | **禁止**删 `data/learning_analytics/learning.db` |
| 运维备忘 | 课前双击 `GameForgeLabServer.exe` 或 §5 启动 API/Kiosk（`0.0.0.0`） |

---

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-07-21 上午 | 首装 PASS · LAN 可开 · Agent 改盘 |
| 2026-07-21 午 | §1–§2 防火墙 + SMB 共享 |
| 2026-07-21 午 | §3–§4 SMB 防火墙 + health PASS |
| 2026-07-21 午 | §7 smoke + 28 pytest PASS → **服务器全面通过** |
| 2026-07-21 晚 | 机房助手 zip 已打：`交付\GameForge-LabHelper-20260721.zip` |
| 2026-07-21 晚 | `_tools\GameForgeLabHelper.exe` + `launch-session-godot.ps1` 发布 PASS（UNC 可读） |
| 2026-07-21 晚 | 新增 `GameForgeLabServer.exe` 机房服务器一键启动（0.0.0.0 · 替代 §5 手敲） |
