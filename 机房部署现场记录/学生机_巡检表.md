# 学生机 · 巡检表（S2-路B · 方案 B）

> **更新方式**：每台机器试点或批量部署后填一行。  
> **手册 §8 自检**：Z: · Godot · 启动脚本 · **机房助手 health** · Kiosk 8080 · API local_share — 六项应全 `True`。  
> **§9 端到端**：至少 1 台完成「助手启动 → 登录 → 试玩自动开 Godot → 首轮关窗弹榜 → release」。

---

## 学生机操作（按顺序）

### 一次性安装（每台 · 管理员 PowerShell）

> **推荐**：下方 **一键部署**（含 §8 自检 + 启动助手 v1.0.1）。  
> **前提**：服务器 `_tools\Godot\` 已发布（手册 §2.5）。

#### 部署前 30 秒预检（可选 · 先跑这个定位问题）

```powershell
$ServerIp = "10.71.121.18"
Test-NetConnection $ServerIp -Port 445 | Select-Object TcpTestSucceeded
Test-NetConnection $ServerIp -Port 8080 | Select-Object TcpTestSucceeded
try { (Invoke-WebRequest "http://${ServerIp}:8080/kiosk/edu/" -UseBasicParsing -TimeoutSec 5).StatusCode } catch { $_.Exception.Message }
# 445/8080 须 True · 再跑下方一键部署
```

#### 一键部署 v2 + 自检 + 启动助手（推荐 · 已修 net delete 误报）

> **脚本自检说明（2026-07-21）**  
> - 新学生机 **无 Z: 映射** 时，旧脚本的 `net use Z: /delete` 会 stderr「找不到网络连接」+ `$ErrorActionPreference=Stop` → **误中断**；v2 用 `cmd /c` 静默删盘符。  
> - **禁止** 在 `net use Z:` 之前 `Copy-Item \\10.71.121.18\...`（未登录 SMB → 假「路径不存在」）。  
> - 复制源必须是 **`Z:\_tools\`**（与首台 §9 PASS 相同链路）。  
> - `deploy --skip-net-use`：Z: 已由脚本映射，避免重复 net use。

```powershell
# ========== 仅改这两行 ==========
$ServerIp = "10.71.121.18"
$NetUser  = "10.71.121.18\TEST1"

$ErrorActionPreference = "Stop"
$ShareUnc    = "\\$ServerIp\GameForgeWorkspace"
$HelperLocal = "C:\GameForge\GameForgeLabHelper.exe"
$GodotLocal  = "C:\Godot\Godot_v4.6.3-stable_win64.exe"
$ToolsHelper = "Z:\_tools\GameForgeLabHelper.exe"
$ToolsPs1    = "Z:\_tools\launch-session-godot.ps1"
$ToolsGodot  = "Z:\_tools\Godot\Godot_v4.6.3-stable_win64.exe"

Write-Host "=== GameForge 学生机一键部署 v2 ===" -ForegroundColor Cyan

# [0] 本机 SMB 客户端防火墙
Enable-NetFirewallRule -DisplayGroup "文件和打印机共享" -ErrorAction SilentlyContinue | Out-Null

# [1] 映射 Z:（删旧映射：无 Z: 时忽略，不报错）
$prevEap = $ErrorActionPreference
$ErrorActionPreference = "SilentlyContinue"
cmd /c "net use Z: /delete /y >nul 2>&1" | Out-Null
$ErrorActionPreference = $prevEap

Write-Host "[1/5] 正在映射 Z: -> $ShareUnc （请输入 SMB 密码）" -ForegroundColor Yellow
& net.exe use "Z:" $ShareUnc "/persistent:yes" "/user:$NetUser"
if ($LASTEXITCODE -ne 0) {
  throw "net use Z: 失败 exit=$LASTEXITCODE · 查: 445 通否 · 账号 $NetUser · 服务器 SMB 共享 GameForgeWorkspace"
}
if (-not (Test-Path "Z:\")) { throw "Z: 映射后仍不可用" }
Write-Host "[1/5] Z: OK" -ForegroundColor Green

# [2] 确认共享 _tools 三件套（与首台 §9 相同前提）
foreach ($p in @($ToolsHelper, $ToolsPs1, $ToolsGodot)) {
  if (-not (Test-Path $p)) {
    throw "共享缺少: $p · 请在服务器发布 _tools（助手 v1.0.1 + §2.5 Godot）"
  }
}
Write-Host "[2/5] Z:\_tools 三件套 OK" -ForegroundColor Green

# [3] 复制到本机
New-Item -ItemType Directory -Force -Path "C:\GameForge" | Out-Null
Copy-Item $ToolsHelper $HelperLocal -Force
Copy-Item $ToolsPs1 "C:\GameForge\launch-session-godot.ps1" -Force
Write-Host "[3/5] 已复制到 C:\GameForge\" -ForegroundColor Green

# [4] deploy：装本机 Godot + lab_helper.json（不再 net use）
& $HelperLocal deploy --skip-net-use
if ($LASTEXITCODE -ne 0) { throw "deploy 失败 exit=$LASTEXITCODE" }
Write-Host "[4/5] deploy OK" -ForegroundColor Green

# [5] §8 六项 + 启动助手
$rows = @(
  [pscustomobject]@{ Check = "Z: 映射"; Pass = (Test-Path "Z:\") }
  [pscustomobject]@{ Check = "Godot exe"; Pass = (Test-Path $GodotLocal) }
  [pscustomobject]@{ Check = "lab_helper.json"; Pass = (Test-Path "C:\GameForge\lab_helper.json") }
  [pscustomobject]@{ Check = "启动脚本"; Pass = (Test-Path "C:\GameForge\launch-session-godot.ps1") }
  [pscustomobject]@{ Check = "助手 exe"; Pass = (Test-Path $HelperLocal) }
)
try {
  $k = Invoke-WebRequest "http://${ServerIp}:8080/kiosk/edu/" -UseBasicParsing -TimeoutSec 8
  $rows += [pscustomobject]@{ Check = "Kiosk 8080"; Pass = ($k.StatusCode -eq 200) }
} catch { $rows += [pscustomobject]@{ Check = "Kiosk 8080"; Pass = $false } }
try {
  $a = Invoke-RestMethod "http://${ServerIp}:8000/health" -TimeoutSec 8
  $rows += [pscustomobject]@{ Check = "API local_share"; Pass = ($a.play_launch_mode -eq "local_share") }
} catch { $rows += [pscustomobject]@{ Check = "API local_share"; Pass = $false } }
$rows | Format-Table Check, Pass -AutoSize
if ($rows.Pass -contains $false) { throw "§8 自检未全过 · 见上表" }

Get-Process GameForgeLabHelper -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
Start-Process $HelperLocal
Start-Sleep -Seconds 2
$h = Invoke-RestMethod http://127.0.0.1:17890/health -TimeoutSec 5
Write-Host "[5/5] 助手 health: ok=$($h.ok) version=$($h.version)" -ForegroundColor Green
if ($h.version -ne "1.0.1") { Write-Warning "期望 version=1.0.1 · 请更新服务器 _tools 后重跑 Copy-Item 段" }
Write-Host "部署完成 → http://${ServerIp}:8080/kiosk/edu/" -ForegroundColor Cyan
```

#### 精简版（手动分步 · 与 v2 等价）

```powershell
Enable-NetFirewallRule -DisplayGroup "文件和打印机共享" -ErrorAction SilentlyContinue | Out-Null
cmd /c "net use Z: /delete /y >nul 2>&1"
net use Z: \\10.71.121.18\GameForgeWorkspace /persistent:yes /user:10.71.121.18\TEST1
Test-Path Z:\_tools\GameForgeLabHelper.exe
New-Item -ItemType Directory -Force -Path "C:\GameForge" | Out-Null
Copy-Item "Z:\_tools\GameForgeLabHelper.exe" "C:\GameForge\GameForgeLabHelper.exe" -Force
Copy-Item "Z:\_tools\launch-session-godot.ps1" "C:\GameForge\launch-session-godot.ps1" -Force
C:\GameForge\GameForgeLabHelper.exe deploy --skip-net-use
Start-Process "C:\GameForge\GameForgeLabHelper.exe"
Invoke-RestMethod http://127.0.0.1:17890/health
```

### 每课时上课前（学生或教师）

1. 双击 **`C:\GameForge\GameForgeLabHelper.exe`**（黑窗口保持开着，勿关）
2. 或管理员 PowerShell：

```powershell
Start-Process "C:\GameForge\GameForgeLabHelper.exe"
Start-Sleep -Seconds 2
Invoke-RestMethod http://127.0.0.1:17890/health
```

期望输出含 `ok : True`。

### 上课时（学生浏览器）

1. 打开 http://10.71.121.18:8080/kiosk/edu/
2. 登录 → 选品类 → 制作完成 → **开始试玩**
3. Godot **自动弹出**（无需手输路径）
4. 玩完 **关 Godot** → 网页 **首轮**自动弹今日榜单
5. 再试玩 / AI 改后关 Godot → **不再**自动弹榜（LB-2）
6. 下课点 **回主页 release**

---

## 统一配置（全班相同）

| 项 | 值 |
|----|-----|
| 映射盘符 | `Z:` |
| UNC | `\\10.71.121.18\GameForgeWorkspace` |
| Godot（本机） | `C:\Godot\Godot_v4.6.3-stable_win64.exe` |
| Godot（共享安装源） | `Z:\_tools\Godot\Godot_v4.6.3-stable_win64.exe` |
| 机房助手（共享安装源） | `Z:\_tools\GameForgeLabHelper.exe` |
| 机房助手（本机） | `C:\GameForge\GameForgeLabHelper.exe` |
| 助手 health | `http://127.0.0.1:17890/health` |
| 启动脚本 | `C:\GameForge\launch-session-godot.ps1` |
| 浏览器入口 | http://10.71.121.18:8080/kiosk/edu/ |

**映射命令（可 GPO）：**

```powershell
net use Z: \\10.71.121.18\GameForgeWorkspace /persistent:yes /user:10.71.121.18\TEST1
```

---

## TEST1 是什么？（易混 · 必读）

| 问题 | 答案 |
|------|------|
| **TEST1 是什么？** | 文档里的**示例名**，指教室服务器 `10.71.121.18` 上的一个 **Windows 本地用户**，用来登录 **SMB 文件共享**（映射 `Z:` → `GameForgeWorkspace`）。 |
| **和浏览器登录有关吗？** | **无关**。网页里学生注册/登录走 GameForge API（`learning.db`）；TEST1 只用于 `net use Z:` / `deploy --net-user`。 |
| **全班要用同一个吗？** | **可以**。机房 S2-路B 设计为全班共用 **一个** SMB 只读/读写账号映射 `Z:`，不必每人一个 Windows 账号。 |
| **脚本里写什么？** | 写成 **`10.71.121.18\<服务器上真实存在的用户名>`** · 首台 §9 能过说明你们现场已有可用账号；若实际不是 TEST1，把脚本两行 `$NetUser` 改成真实用户名即可。 |
| **密码谁管？** | 服务器管理员在 `10.71.121.18` 上创建/维护该 Windows 用户；`net use` 时在本机**输入一次**，`/persistent:yes` 会记住映射。 |
| **权限要求** | 对该共享至少 **读**（读 `_tools`、读 workspace 会话目录）；理想态再配合 NTFS ACL 做用户隔离（可选 P1）。 |

**如何查服务器上可用用户名（RDP 到 10.71.121.18）：**

```powershell
Get-LocalUser | Where-Object Enabled -eq $true | Select-Object Name
# 或你们 IT 提供的机房专用账号名（可能与 TEST1 不同）
```

---

## 巡检记录

| # | 机器标识（座位/IP/主机名） | §8 六项 Pass | §9 端到端 | 备注 | 日期 |
|---|---------------------------|--------------|-----------|------|------|
| 1 | 首台试点机（§9 全链路 PASS） | ✅ | ✅ | v1.0.1 · AI改+release · 见 [`2026-07-21_§9验收与助手v1.0.1热修.md`](./2026-07-21_§9验收与助手v1.0.1热修.md) | 2026-07-21 |
| | | | | | |
| | | | | | |

**§8 Pass 判定：** 六项自检表全 `True`（含助手 `/health`）。  
**§9 判定：** 首轮关 Godot 自动弹榜 + release 后服务器 session 目录删除。

---

## 批量部署脚本位置

见 [`开发文档/7.21_教学机房S2路B_PowerShell按序执行手册_2026-07-21.md`](../开发文档/7.21_教学机房S2路B_PowerShell按序执行手册_2026-07-21.md) **§8**（整段复制到学生机管理员 PowerShell）。

---

## Win11 专业版 SMB 连接上限（2026-07-21 实测）

| 项 | 说明 |
|----|------|
| **现象** | 第 21 台左右 `net use Z:` 报 **系统错误 71** · 资源不足 |
| **原因** | **Windows 11 专业版** 作 SMB 服务器时，**同时 inbound 连接约 20**（`Get-SmbSession` Count≈20） |
| **课堂策略** | 25 人课 · **≤20 台** 同时 persistent `Z:` + 本机试玩可接受；满员时其余机仅浏览器或排队 |
| **监控** | 服务器：`Get-SmbSession \| Measure-Object` · 课前勿超 20 |
| **架构** | 可选 **一台学生机兼服务器**（见 [`7.21_教学机房_学生机兼服务器_Cursor开工提示词`](../开发文档/7.21_教学机房_学生机兼服务器_Cursor开工提示词_2026-07-21.md)）· `$ServerIp` 改本机 LAN IP |

**脚本化 deploy v2**（改 IP/账号即可）：

```powershell
powershell -ExecutionPolicy Bypass -File "E:\project\GameForge-K12\05-工具脚本\deploy_lab_student_v2.ps1" `
  -ServerIp "<教室服务器LAN_IP>" `
  -NetUser "<LAN_IP>\<Windows_SMB用户名>"
```

---

## 常见问题（现场）

| 现象 | 处理 |
|------|------|
| `net use` **系统错误 71** | SMB 连接满（Win11 Pro ~20）· 减同时映射台数或换 Win Server |
| `Copy-Item \\10.71.121.18\...` 找不到路径 | **新学生机**：先 `net use Z:`，再 `Copy-Item Z:\_tools\...`（勿 UNC 直拷） |
| `net use Z: /delete` 找不到网络连接 | **正常**：新机器本无 Z: · 用 v2 脚本（`cmd /c ... >nul` 静默删）· 勿设 Stop 后直接 delete |
| `net use` 映射失败 | 445 不通 · 账号错 · 服务器 SMB · 本机「文件和打印机共享」防火墙 |
| 网页开但 AI 不工作 | 服务器 `api_base` 是否为 LAN IP（已 OK） |
| Godot 找不到工程 | 检查 Z: 与 `project_path` 是否换成 `Z:\users\...` |
| `deploy` 报共享盘无 Godot | 服务器先执行 §2.5 发布 Godot 到 `_tools\Godot\` |
| 试玩不自动开 Godot | 确认 `GameForgeLabHelper.exe` 在跑 · `/health` ok |
| 关 Godot 不弹榜 | 须 Kiosk 三文件已同步 + 本会话**首轮**试玩（LB-2） |
| release 失败 | 先关 Godot |
