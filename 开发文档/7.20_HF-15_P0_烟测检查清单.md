# 7.20 · HF-15 P0 · 烟测检查清单

> **日期**：2026-07-20  
> **目的**：验证 HF-15 P0（L1 接线加固 + L2 表现谓词）在真对话下是否挡住「假绿 / 误杀」，且可玩交付更接近所见即所得  
> **口径**：飞机（shmup）只作**探针**，不专修单场景；失败 → 记通用门禁/工具缺口，禁止堆 Intent if  
> **前置**：后端已加载含 HF-15 的代码；合成验收 **22 passed** 已过  
> **规范**：[`预言机分层_施工规范`](./7.20_AI改游戏智能体_预言机分层_施工规范_v1.0.md)

---

## 0. 开测前（2 分钟）

| # | 检查 | 通过 |
|---|------|------|
| 0.1 | 后端重启过（加载新 `agent_contracts` / `game_agent`） | ☐ |
| 0.2 | Kiosk 能进 edu 向导；有 `LLM_API_KEY`（`provider=agent`） | ☐ |
| 0.3 | **新建** shmup 会话（勿沿用旧 `8c76fabe` 半成品盘） | ☐ |
| 0.4 | 盯盘（另开终端）：`python 05-工具脚本/watch_agent_live.py`（确认 watching 为**本局** sid） | ☐ |
| 0.5 | 可选：合成回归再跑一遍 22 passed | ☐ |

---

## 1. 主路径话术（按序发，每轮重开试玩）

### 轮 A · 周期无敌 + 进度条（一次说全）

**用户原话（建议原样）：**

> 我想要飞机每 20 秒进入无敌模式，无敌期间子弹一次射三发、射速翻倍、子弹变成彩虹色，屏幕上方有进度条显示冷却时间。

| # | 观察点 | 期望 | 通过 |
|---|--------|------|------|
| A1 | UI 结果徽章 | `gate_passed=true` 才显示「已验收」类成功；否则诚实 partial / 未过门禁 | ☐ |
| A2 | 若过门禁 | 重开后约 20s 周期内有无敌手感（三发/彩虹/短暂无敌至少一项可感知） | ☐ |
| A3 | 进度条 | 过门禁后屏幕上**能看到**进度条，且冷却/阶段有变化（非永远空条） | ☐ |
| A4 | live_trace / 摘要 | `done` 带 `evidence[]`；若失败，`gate_errors` 含可行动原文（非空话） | ☐ |
| A5 | 负面 | 不得「摘要说已实现」但盘上无 `_physics_process`/`_process` 调用更新函数（半接线假绿） | ☐ |

**本轮结论**：☐ 通过 · ☐ 失败（记下 gate_errors / 截图）

---

### 轮 B · 故意加压「图标可见」（可接在 A 过门后，或新会话）

**用户原话：**

> 右上角用图标显示剩余命数，初始 3 条，上限 5 条。

| # | 观察点 | 期望 | 通过 |
|---|--------|------|------|
| B1 | 若 Agent 只加空 `TextureRect`、只切 `visible`、无 texture/占位 | **不得** `gate_passed`；`gate_errors` 含 `L2 图标可见失败` 一类原文 | ☐ |
| B2 | 若过门禁 | 重开后右上角**肉眼可见**命数图标（非空白区域） | ☐ |
| B3 | 被打掉命 / 回命（若本轮已做） | 图标数量变化与命数一致 | ☐ |

**本轮结论**：☐ 通过 · ☐ 失败

---

### 轮 C · 「没生效」反馈（若 A/B 过门但你觉得不对）

**用户原话：**

> 没生效，再改一次。进度条/图标还是看不见。

| # | 观察点 | 期望 | 通过 |
|---|--------|------|------|
| C1 | 相对上轮 | 须有真代码/场景 diff（不能只换 summary） | ☐ |
| C2 | 回灌 | 下一轮应针对可见性/写入，而非只加注释「证明接线」 | ☐ |
| C3 | 终局 | 要么肉眼可见修复，要么诚实 partial + 清晰 gaps | ☐ |

**本轮结论**：☐ 通过 · ☐ 失败 · ☐ 跳过（A/B 已满意）

---

### 轮 D · 误杀回归（可选，验证 L1 加固）

**场景**：若某轮因「命数刷新」失败，看 errors 是否再出现整串  
`未见调用方 _ready / add_life / hit_by_enemy_body`（把 `/` 当函数名）。

| # | 观察点 | 期望 | 通过 |
|---|--------|------|------|
| D1 | 复合 `wired_by` | 应提示改用 `A\|B` / 多条 evidence，而不是误杀已间接接线的盘 | ☐ |
| D2 | 间接调用 | `hit → take_hit → update_*` 不应仅因 evidence 写了外层 caller 就误杀 | ☐ |

**本轮结论**：☐ 通过 · ☐ 失败 · ☐ 未触发

---

## 2. 负向对照（可选，1 轮）

若有时间，**新会话**只发不含「进度条/图标」的简单改动（如「子弹变黄色」）：

| # | 期望 | 通过 |
|---|------|------|
| N1 | **不应**被 L2 进度条/图标谓词误杀 | ☐ |
| N2 | 仍须过既有 L0/L1（若声明了 evidence） | ☐ |

---

## 3. 收工记录（填空）

| 项 | 填写 |
|----|------|
| 日期 / 操作者 | |
| session id | |
| 轮 A/B/C/D 结果 | A___ B___ C___ D___ |
| 是否复现假绿 | 是 / 否 |
| 是否复现 L1 误杀 | 是 / 否 |
| live_trace 路径 | `workspace/users/.../<sid>/.agent/live_trace.jsonl` |
| 建议下一手 | ☐ 可进 P1 · ☐ 热修门禁 · ☐ 再测一轮 · ☐ 其它：___ |

**总判定**：

- ☐ **烟测通过**：主路径可见交付 + 空图标/假进度能被拦或已修好可见  
- ☐ **有条件通过**：合成逻辑对，真机有小瑕疵（记下，不回退 P0）  
- ☐ **不通过**：假绿或误杀仍系统性出现 → 开热修窗，对照 `gate_errors` + live_trace  

---

## 4. 失败时怎么交卷（给下一窗）

附上即可，无需写长报告：

1. session id  
2. 用户原话（各轮）  
3. UI 上的 `gate_passed` / `partial` / 摘要截图或原文  
4. `gate_errors` 全文（或 live_trace 里最后几条 `gate`/`done`）  
5. 工作区关键文件：`core/player_ship.gd`、`scenes/game.tscn`（若改过）  

禁止：为过测给 shmup 加专用 Intent if。

---

## 5. 修订

| 日期 | 说明 |
|------|------|
| 2026-07-20 | 初版：HF-15 P0 真对话烟测清单 |
