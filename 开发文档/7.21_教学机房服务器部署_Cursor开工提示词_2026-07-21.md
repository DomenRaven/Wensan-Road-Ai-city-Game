# 7.21 · 教学机房服务器部署 · Cursor 开工提示词

> **日期**：2026-07-21  
> **用途**：粘贴到**服务器端 Cursor** 新对话，执行首装 / 复装 / 验收  
> **场景**：Windows 服务器 · 交付包已解压至 `E:\project\GameForge-K12` · **S2-路B + S4** 机房模式  
> **配套**：[`服务器部署_AI智能体自动部署手册_v1.3.md`](./服务器部署_AI智能体自动部署手册_v1.3.md) · [`7.20_教学机房_S2路B_部署检查清单_草案.md`](./7.20_教学机房_S2路B_部署检查清单_草案.md) · [`请先读_展厅部署与操作.md`](../请先读_展厅部署与操作.md)

---

## 0. 使用说明

1. 在服务器打开 Cursor，**新开对话**。  
2. 复制下方 **「整段开工词」** 粘贴发送。  
3. 按 Agent 回报补齐：`GODOT_PATH`、`LLM_API_KEY`、workspace 共享路径（若 IT 已配）。  
4. 验收通过后，Agent 应输出 **`7.21_服务器部署验收_2026-07-21.md` 摘要**（可另存为文档）。

---

## 1. 整段开工词（复制起点 → 复制终点）

<!-- 复制起点 -->

```
你是 GameForge K12 **服务器部署 Agent**（2026-07-21 · 教学机房首装）。
工作目录：`E:\project\GameForge-K12`（Windows 服务器）。
目标：解压/校验交付包 → 安装依赖 → 配 `.env`（机房模式）→ 启动服务 → 局域网可访问 → 健康验收 → 输出部署报告。

## 你必须先读（按序，再动手）

1. `E:\project\GameForge-K12\请先读_展厅部署与操作.md`
2. `E:\project\GameForge-K12\开发文档\服务器部署_AI智能体自动部署手册_v1.3.md`（全文）
3. `E:\project\GameForge-K12\开发文档\7.20_教学机房_S2路B_部署检查清单_草案.md`
4. `E:\project\GameForge-K12\backend\.env.example`

## 环境事实（已确认）

| 项 | 值 |
|---|---|
| 服务器 OS | Windows |
| 项目根 `$ROOT` | `E:\project\GameForge-K12` |
| 交付来源 | `GameForge-K12-v1.2-server-20260721.zip` |
| 部署模式 | **教学机房 · S2-路B + S4**（非展厅 server 试玩） |
| 服务器内网 IP（LAN 用） | `10.71.121.18`（若 `ipconfig` 不符则以实测为准） |
| 产品版本 | 见 `$ROOT\VERSION`（应为 1.2） |

## 红线（违反即停）

- **禁止**修改 `templates/*/core/**`（玩法冻结源）
- **禁止**把 `LLM_API_KEY`、密码提交进 Git
- **禁止**为排障删除 `data/learning_analytics/learning.db`（含 auth + 学情）
- **禁止**假定「手机扫码下载证书」已可用（二维码未实装；证书用本机保存 PNG）
- **禁止**跳过 `/health` 就声称部署成功
- 未要求前：**不要** `git commit` / `git push`

## 第 0 步 · 确认目录结构

```powershell
Set-Location "E:\project\GameForge-K12"
Get-ChildItem
```

**若当前目录只有 `.zip` 没有 `backend/`：**

```powershell
Expand-Archive -Path "E:\project\GameForge-K12\GameForge-K12-v1.2-server-20260721.zip" `
  -DestinationPath "E:\project\GameForge-K12" -Force
```

**必须存在：** `backend/`、`kiosk/`、`templates/`、`config/`、`assets/`、`05-工具脚本/`、`VERSION`

## 第 1 步 · 前置软件（缺则安装或报缺）

| 软件 | 要求 |
|------|------|
| Python | 3.11+，已加入 PATH |
| Godot | **4.6 Standard**（记下 exe 全路径，填 `GODOT_PATH`） |
| Redis | 推荐；`05-工具脚本\install_redis.ps1` + `run_redis.ps1` |

```powershell
python --version
Get-ChildItem -Path C:\,E:\ -Recurse -Filter "Godot*.exe" -ErrorAction SilentlyContinue | Select-Object -First 5 FullName
```

## 第 2 步 · 写 `backend\.env`（机房必写项）

若不存在：`Copy-Item backend\.env.example backend\.env -Force`

**至少**包含：

```env
REDIS_URL=redis://127.0.0.1:6379/0
ALLOW_MEMORY_FALLBACK=true

MAX_SESSIONS=70
MAX_CONCURRENT_AGENTS=6
AGENT_QUEUE_WAIT_SEC=90

PLAY_LAUNCH_MODE=local_share

GODOT_PATH=C:\Godot\Godot_v4.6.3-stable_win64.exe

LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=<向管理员索取>
LLM_MODEL=deepseek-chat
LLM_TIMEOUT_SEC=30

DEPLOYMENT_SERVER_OS=Windows
DEPLOYMENT_TERMINAL_LAYOUT=lab
```

**workspace 默认（本机，首装/自测）：** `E:\project\GameForge-K12\workspace`（不设 `WORKSPACE_DIR` 时即用此目录）

**若 IT 已配共享盘（整班学生机 Godot 试玩时再改）：**

```env
# WORKSPACE_DIR=\\NAS\gameforge\workspace
```

## 第 3 步 · 一键部署

```powershell
Set-Location "E:\project\GameForge-K12"
Set-ExecutionPolicy -Scope Process Bypass -Force
powershell -ExecutionPolicy Bypass -File .\05-工具脚本\deploy_exhibition.ps1 `
  -GodotPath "<GODOT_PATH 实际路径>" `
  -NoBrowser
```

## 第 4 步 · 局域网让学生机访问

### 方案 A（推荐）· Nginx 9080

```powershell
.\05-工具脚本\kiosk_lan_nginx.ps1 -Port 9080 -BackendPort 8000
```

改 `config\kiosk_edu_spec.json`：`"api_base": "http://10.71.121.18:9080/api"`

学生入口：`http://10.71.121.18:9080/kiosk/edu/`

防火墙入站放行 **9080**。

### 方案 B · 直连 8000/8080

- 后端 `--host 0.0.0.0` · Kiosk `--bind 0.0.0.0`
- `api_base`：`http://10.71.121.18:8000`

## 第 5 步 · 健康验收

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
Invoke-WebRequest http://127.0.0.1:8080/kiosk/edu/ -UseBasicParsing | Select-Object StatusCode
```

期望：`max_sessions>=70` · `play_launch_mode=local_share`

```powershell
E:\project\GameForge-K12\backend\.venv\Scripts\python.exe `
  E:\project\GameForge-K12\05-工具脚本\smoke_7_20_nb_p0.py
```

可选 Agent 簇：

```powershell
Set-Location E:\project\GameForge-K12\backend
.\.venv\Scripts\python.exe -m pytest tests/test_hf15_1_hollow_done.py tests/test_hf15_oracle_layers.py tests/test_hf14_evidence.py -q
```

期望：**28 passed**

## 第 6 步 · S2-路B 抽样

1. LAN 打开 edu 入口 → 登录 → 选品类 → AI 改一句 → 试玩  
2. 期望：`local_share` + `project_path`，**服务器无 Godot 进程**  
3. release 后 workspace 可清；学情仍在 `learning.db`

## 第 7 步 · 输出部署报告

```text
7.21_服务器部署验收_2026-07-21.md（摘要）

- ROOT / Godot / health / LAN 入口 / api_base
- smoke_7_20_nb_p0: PASS/FAIL
- local_share 试玩: PASS/FAIL
- 需 IT: 共享盘 ACL、学生机盘符
- 二维码: 未实装
```

## 开始执行

从第 0 步做起，每步贴关键输出；缺 Key/路径则列出需人工项后继续。
```

<!-- 复制终点 -->

---

## 2. 部署前人工清单（服务器外）

| # | 项 | 负责 |
|---|----|------|
| 1 | Godot 4.6 Standard 安装路径 | 运维 / IT |
| 2 | `LLM_API_KEY` 或校内 LLM 网关 | 管理员 |
| 3 | 学生机映射 `workspace` 共享盘 | IT |
| 4 | 防火墙放行 9080（或 8000+8080） | IT |
| 5 | 课堂约定：先关 Godot 再 release | 教师 |

---

## 3. 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 初版：机房 S2-路B + S4 · `$ROOT=E:\project\GameForge-K12` · 整段 Cursor 开工词 |
| 2026-07-21 | 服务器路径更正为 `E:\project\GameForge-K12`；补充默认 workspace 本机路径 |
