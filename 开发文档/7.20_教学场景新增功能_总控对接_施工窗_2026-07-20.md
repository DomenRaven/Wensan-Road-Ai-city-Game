# 7.20 · 教学场景新增功能 · 总控对接（施工窗）

> **日期**：2026-07-20  
> **用途**：原对话保留为**总控 / 测试 / 验收**；新开窗口为**施工窗**。  
> **需求**：[`7.20_教学场景新增功能_需求说明_v1.4.md`](./7.20_教学场景新增功能_需求说明_v1.4.md)  
> **施工**：[`…_施工规范_v1.0.md`](./7.20_教学场景新增功能_施工规范_v1.0.md) · [`…_文件映射表_v1.0.md`](./7.20_教学场景新增功能_文件映射表_v1.0.md)  
> **粘贴**：[`…_开工提示词_v1.0.md`](./7.20_教学场景新增功能_开工提示词_v1.0.md)

---

## 0. 一句话现状

需求 **v1.4 已确认**；开工材料（施工规范 / 映射表 / 开工词）已齐。  
施工窗执行 **P0=W1–W7**；本总控窗负责范围裁定、红线抽查、H0–H6 复验，**不代替施工窗改大段业务代码**（除非用户要求总控热修）。

---

## 1. 双窗分工

| | **施工窗（新开）** | **总控窗（本对话）** |
|--|-------------------|----------------------|
| 读 | 开工词 → 需求 §0.2/§1.1 → 施工规范 → 映射表 | 同上 + 施工窗汇报 |
| 做 | 实现 W1–W7；单测；填进度表 §5 | 验收 H*；查 `templates/*/core` 未被改；裁定 OQ/范围 |
| 不做 | 擅自开 P1（F6 等）；commit（除非用户要） | 不静默扩需求 |

---

## 2. 新窗怎么开（给用户）

1. 同仓库根目录，**新开 Cursor Agent 对话**（不必新建磁盘目录）。  
2. 打开 [`开工提示词`](./7.20_教学场景新增功能_开工提示词_v1.0.md)，复制「提示词正文」整段粘贴发送。  
3. 施工窗应先复述 P0 计划再动代码。  
4. 每完成 W 包或声称 P0 完成时，回到**本总控窗**说：「请按 H0–H6 验收」。

---

## 3. 总控验收清单（P0）

对照施工规范 §4.2：

| ID | 查什么 | 通过标准 |
|----|--------|----------|
| H0 | 初始页 | 游客 / 登录两线 |
| H0b | 登录命名 | 无「你的名字是？」；shmup → 含「街机飞机射击」 |
| H1 | F1 | 证书保存后可浏览真实 workspace（含 scenes 文本） |
| H2 | F2 | Diff 绿/红+斜体；有注释 |
| H3 | F3 | 星级后下一轮上下文含 rating |
| H4 | 隔离 | 两账号互不可读 |
| H5 | F5 | release 后长回复+评价仍在 SQLite |
| H6 | S4 | 超帽有排队/繁忙提示 |
| 红线 | git / 抽查 | 无 `templates/*/core` 业务改动 |
| 展厅 | 游客 + `play_launch_mode=server` | 仍能服务器起 Godot（或与现网一致） |

**不验收为 P0 失败**：机房 70 台实机同开、F6 问卷、心得墙、注解美学满分。

---

## 4. 权威序（争议）

1. 需求 §0.2 已确认列  
2. 需求 §1.1 现网基线  
3. 施工规范  
4. 文件映射表  
5. 施工窗临场实现  

---

## 5. 待补（非堵 P0）

| 文档 | 说明 |
|------|------|
| `7.20_服务器并发与部署说明_v1.0.md` | OQ-11：盘符、ACL、本机启动脚本、S4 参数建议值 |

---

## 6. 验收记录（总控填写）

> **完整归档（含非阻塞标注 NB-*）**：[`7.20_教学场景新增功能_验收记录_P0_总控_2026-07-20.md`](./7.20_教学场景新增功能_验收记录_P0_总控_2026-07-20.md)  
> 以下为摘要；新增包验收请同步写入上述验收记录文档。

### 2026-07-20 · W1 + W2

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_auth_w1` + `test_learning_w2` | ✅ **10 passed** | 约 20s |
| W1 对照需求 | ✅ | 注册登录、游客路径、登录 `users/{uid}/{sid}`、403 隔离、单会话接管、F4-N3 品类名、创作者名锁定 |
| W2 对照需求 | ✅ | `compose_agent_reply_full`=F5-L2；库在 `learning_analytics`；release 后回合仍在；nl-patch 回传 `turn_id`（代码已接） |
| H0 初始页 | ✅ 代码验收 | `entry-gate.js` + `index.html` 入口；**浏览器点选冒烟未在本机开服复跑**（建议开服点一次） |
| H0b 登录命名 | ✅ 代码+单测 | `isLoginMode` 跳过 creator；`default_work_display_name` 断言「街机飞机射击」 |
| H5 学情骨架 | ✅ 自动化 | 直接 `record_agent_turn` + release；**缺口**：尚无「HTTP nl-patch→库」一条龙单测（落库失败被 swallow 成 `turn_id=""`） |
| H1 / H2 / H4 / H6 | ⏸ | 见后续 W 包验收 |

**总控结论**：W1、W2 **通过验收**，可继续 W3。  
**跟进建议（非阻塞）**：补一条 stub nl-patch 端到端落库单测；现场开 `kiosk/edu/` 点一次游客/登录。

### 2026-07-20 · W3

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_turn_rating_w3` | ✅ **3 passed** | 约 7s |
| 评价 API + revision | ✅ | score 2→4，label「比较不满意/比较满意」，revision 1→2 |
| 未评价不注入 | ✅ | `get_previous_turn_user_rating` 为 None |
| H3 注入 | ✅ | monkeypatch `run_game_agent`：`previous_turn_user_rating.score==2`；JSON 系统注入非用户原话 |
| UI 星级区 | ✅ 代码验收 | `nl-patch-dialog.js` 结果区星/跳过/改评；五档文案与需求一致 |
| stub 无 Key 路径 | ℹ 标注 | 注入走 `run_game_agent`（有 Key）；无 Key stub 不跑 Agent——**符合「有 Key 才 Agent」**，不挡验收 |

**总控结论**：W3 **通过验收**，可继续 W4。

### 2026-07-20 · W4

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_turn_diff_w4` | ✅ **3 passed** | 约 2.4s |
| 快照范围 | ✅ | `config/`·`core/`·`scenes/`·`ai_sandbox/` 文本后缀 |
| Diff 语义 | ✅ | added/modified/deleted；unified `+/-`；无变更「无净变更」 |
| 持久 + release 回看 | ✅ | `blobs/{turn_id}/diff.json`；release 后 GET 仍 200；不在 learned_skills |
| H2 UI | ✅ 代码验收 | 入口按钮、文件 Tab、Diff/改后全文；CSS 绿底增行、红底+斜体删行 |
| 注释 | ✅ | overview=长回复；文件 note 规则模板，无另调 LLM |
| 端到端 nl-patch 实改文件 | ℹ 标注 | 单测用手动 before/after + API；生产路径 `nl_patch` 已接 snapshot→save（未再开 LLM 实改一条龙） |

**总控结论**：W4 **通过验收**，可继续 W5。

### 2026-07-20 · W5

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_workspace_tree_w5` | ✅ **2 passed** | 约 14s |
| tree + file 白名单 | ✅ | scenes 可读；assets 只列不预览；穿越拒绝 |
| 证书门闩 | ✅ | `certificate_saved`；B6/B7 按钮未保存置灰 |
| UI + 注解 | ✅ 代码验收 | `code-browser.js`；`config/code_annotations/` |
| H1 浏览器冒烟 | ℹ 标注 | 见验收记录 **NB-07**（不挡） |
| API 未硬校验证书 | ℹ 标注 | 见 **NB-09**（可选加固） |

**总控结论**：W5 **通过验收**，可继续 W6（S4 排队）。  
**跟进建议（非阻塞）**：开服点一次「存证书 → 查看游戏代码 → scenes」。

### 2026-07-20 · W6

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_agent_queue_w6` | ✅ **4 passed** | 约 6.8s |
| 帽 + 排队 + 互斥 | ✅ | 满员 503「人数已满」；session/user busy → 429 |
| health / 配置 | ✅ | 队列字段暴露；`MAX_*` 见 `.env.example` |
| FE 文案 | ✅ 代码验收 | loading + 429/503 中文提示 |
| H6 浏览器 / 多 worker / 默认 sessions | ℹ 标注 | 见验收记录 **NB-10～NB-12**（不挡） |

**总控结论**：W6 **通过验收**，可继续 W7（S2-路B 钩子）。  
**部署提醒**：机房须设 `MAX_SESSIONS≥70`；单进程或接受进程内帽限制。

### 2026-07-20 · W7

| 项 | 结果 | 说明 |
|----|------|------|
| 自动 `test_play_launch_w7` + `test_play_launch` | ✅ **7 passed** | 约 12.9s |
| `local_share` | ✅ | 不起服务器 Godot；`project_path` + `ready_for_local_godot`；status `server_godot=false` |
| 默认 `server` | ✅ | 展厅回归单测仍走 Launcher |
| FE / 清单 | ✅ | 本机路径提示、不误轮询；[`S2路B检查清单草案`](./7.20_教学机房_S2路B_部署检查清单_草案.md) |
| 机房 IT | ℹ 标注 | 见验收记录 **NB-14**（不挡） |

**总控结论**：W7 **通过验收**；**P0（W1–W7）代码侧齐套**。  
**部署**：机房 `PLAY_LAUNCH_MODE=local_share`；展厅保持默认或显式 `server`。

### 2026-07-20 · 推荐消项冒烟

| 项 | 结果 | 说明 |
|----|------|------|
| 脚本 | ✅ | `05-工具脚本/smoke_7_20_nb_p0.py` |
| 消项 | ✅ | NB-01 / 03 / 06 / 07 / 10 / 12 → **done** |
| 关闭 | ✅ | NB-04 / 08 → **wontfix** |
| 部署 env | ✅ | `backend/.env` + `.env.example`：`MAX_SESSIONS=70` 等；health 已验 |
| 仍 open | ℹ | 02/05/09/11/13/14/15（可选或 IT） |

**总控结论**：推荐消项已收口；后续见 [`P0消项后开工提示词`](./7.20_教学场景新增功能_P0消项后_开工提示词_v1.0.md)。

---

## 7. 修订

| 日期 | 说明 |
|------|------|
| 2026-07-20 | 首版：双窗分工与 H0–H6 总控清单 |
| 2026-07-20 | W1+W2 总控验收通过（§6） |
| 2026-07-20 | W3 总控验收通过（§6） |
| 2026-07-20 | W4 总控验收通过（§6） |
| 2026-07-20 | 验收明细与 NB-* 迁入独立 [`验收记录`](./7.20_教学场景新增功能_验收记录_P0_总控_2026-07-20.md) |
| 2026-07-20 | W5 总控验收通过（§6） |
| 2026-07-20 | W6 总控验收通过（§6） |
| 2026-07-20 | W7 总控验收通过；P0 代码侧齐套（§6） |
| 2026-07-20 | 推荐消项冒烟通过；增 P0 消项后开工提示词 |
