# 6.24 P1-R · 附录 A · 完整窗口提示词 v1.0

> **主文档**：[`6.24_P1-R_前端与联调收工手册_v1.0.md`](./6.24_P1-R_前端与联调收工手册_v1.0.md)  
> **前置快照**：[`快照/6.24_P1-R_开工前状态快照_v1.0.md`](./快照/6.24_P1-R_开工前状态快照_v1.0.md)  
> **用法**：新开 Cursor 窗口 → 选推荐模型 → **复制对应章节整段** 作为首条消息  
> **约束**：遵守 [`.cursor/rules/p1-edu-frontend.mdc`](../../.cursor/rules/p1-edu-frontend.mdc)

---

## 模型速查

| 窗 | 任务 | 推荐模型 | 可并行 |
|----|------|----------|--------|
| 9 | P1-04 意图词表 | **Composer** | 与窗11 |
| 10 | P1-05/06 B4+B2 | **Sonnet 思考** 或 **Composer** | 等窗9 |
| 11 | P1-08 play/action | **Composer** | 与窗9 |
| 12 | P1-09 Kiosk 分流 | **Auto** | 等窗10 |
| 13 | 收工冒烟+文档 | **Composer** | 等窗9–12 |

---

## A.9 窗9 · P1-04 意图词表接线

**模型**：Composer  
**前置**：P1 窗8 完成 · `intent_genre_lexicon.json` 已存在七款

```text
【6.24 P1-R · 窗9 · P1-04 意图词表 · 完整施工单 v1.0】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：P1-04 仅下列文件（禁止改 kiosk/edu 其他窗 · 禁止改 templates/core）

## 交付
1. config/intent_genre_lexicon.json
   - 七款 genres 齐全：platformer shmup survivor pingpong fighting parkour racing
   - 每款 keywords ≥8 · display_name · reply_template
   - example_chips 七条（与 E2E batch intent 对齐）：
     马里奥闯关 · 我想打飞机 · 割草打怪 · 乒乓球 · 格斗双人 · 跑酷 · 赛车
   - algorithm/tie_break 保持与现文件一致

2. backend/app/routers/intent.py
   - match-genre 以 load_intent_lexicon() 为唯一关键词来源
   - 删除或内联 DEFAULT_INTENT_KEYWORDS 重复（勿两套口径）
   - 评分：keyword 命中 × weight · tie_break 顺序

3. kiosk/edu/steps/b1-intent.js（仅本文件）
   - KEYWORD_MAP / 示例芯片与 lexicon 或 kiosk_edu_spec 同源
   - #intentExamples 展示七款示例（可滚动 chip-row）
   - API 成功时仍走 POST /intent/match-genre；fallback 与 lexicon 一致

4. 可选：config/kiosk_edu_spec.json 增补 intent_example_chips 引用 lexicon

## 必读
- config/intent_genre_lexicon.json（现状）
- backend/app/routers/intent.py
- kiosk/edu/steps/b1-intent.js
- 05-工具脚本/e2e_b_edu_batch.py 各款 intent_text

## 七款 match 验收句
| slug | 测试句 |
|------|--------|
| platformer | 我想玩马里奥闯关 |
| shmup | 我想打飞机 |
| survivor | 割草打怪 |
| pingpong | 乒乓球 |
| fighting | 格斗双人 |
| parkour | 跑酷 |
| racing | 赛车 |

## 验证
- backend 常驻 :8000
- curl/脚本七句 POST /intent/match-genre → matched_genre 全对
- 可选：backend/tests 增补 intent 七款参数化

## 硬禁止
- templates/{slug}/core/**
- kiosk/edu 除 b1-intent.js 外任何文件
- creative_templates / anchors / theater

## 收工报告
- 修改文件列表
- 七句 match 结果表
- lexicon example_chips 最终数组
```

---

## A.10 窗10 · P1-05/06 B4 通用表单 + B2 推荐名

**模型**：Claude Sonnet（思考）或 Composer  
**前置**：窗9 ✅（match-genre 七款可用）· 窗11 ✅（POST play/action 已就绪，**本窗不改 B7**）

```text
【6.24 P1-R · 窗10 · P1-05/06 · B4+B2 · 完整施工单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：仅 kiosk/edu/ 下步骤与样式（可改 edu-styles.css · edu-wizard.js 集成段落）

## 前置上下文（窗9/11 已交付 · 勿重复施工）
- B1 七款 example_chips 已接线；genre 经 match-genre 写入 edu-wizard 的 `genre` 变量，B2/B4 直接复用
- 后端 `POST /sessions/{id}/creative/answers` 已支持 skill_pick → `string[]`（见 creative.py `_validate_answers`）
- `POST /sessions/{id}/play/action` 已就绪（窗11）；`code-highlight.js` 仍用 GET 轮询，**本窗不要求改 B7**
- 总控回归基线：`validate` 7/7 · `e2e_b_edu_batch` 6/6 仍须 PASS（勿为过关改 backend/templates）

## 现状缺口（必读再改）
- `b4-creative.js`：`FALLBACK_TEMPLATES` 仅 platformer 且缺 q_skill；API 失败静默 fallback → 非 shmup 等会错显
- `b4-creative.js`：`render` 仅 `single_choice`；`validate` 未处理 `optional:true`；默认答案逻辑 `q.options[1]` 不适用于 skill_pick
- `b2-name.js`：API 路径已有，失败时 fallback 为通用「星星大冒险」等，须确认主路径稳定命中各款 `name_suggestions`

## 交付
1. kiosk/edu/steps/b4-creative.js
   - loadTemplate(genre)：
     · 必须 `GET /creative/templates/{genre}` 成功才渲染
     · API 失败：面板内明确报错 + `EduSession.log`（**禁止**静默 `FALLBACK_TEMPLATES.platformer`）
     · 可删除或仅 dev 保留 FALLBACK_TEMPLATES（勿用于生产路径）
   - render 按 `question.widget` 分支：
     · `single_choice`：保持 option-card（已有）
     · `skill_pick`：多选芯片 · 读 `question.skill_ids` · `optional:true` 可提交 `[]`
       - 标签：加载 `../../config/optional_skills.json` → `catalog[genre]` 匹配 id→label（无则显示 id）
       - 最多选 2 个（对齐 optional_skills.rules.max_skills_per_session）
       - 样式 class：`skill-chip` / `skill-chip.selected`
   - answers 类型：`single_choice` → string · `skill_pick` → string[]
   - validate：
     · 非 optional 的 single_choice 必选
     · optional skill_pick 无选也可过（`[]` 合法）
     · fighting 仅 4 道 single_choice，无 q_skill
   - 默认预选：single_choice 仍可用中间项 `options[1]`；skill_pick 初始 `[]`（勿对无 options 的题跑 options[1]）
   - submitAnswers：POST `{ answers }` 与 E2E batch 结构一致（skill 为数组）

2. kiosk/edu/steps/b2-name.js
   - getSuggestions(genre)：主路径 `GET /creative/name-suggestions?genre=`
   - 仅 API 失败时用 `kiosk_edu_spec.name_suggestions[genre]`（禁止默认通用名掩盖 API 成功）
   - 芯片 ≥4 · 点击填入 #nameInput
   - 验收：各款芯片须为 creative_templates 原文（见下表），非「星星大冒险」套娃

3. kiosk/edu/edu-wizard.js（集成小改）
   - B4：`creativeAnswers` 支持 `string | string[]`；`validate`/`submitAnswers` 传完整对象
   - B2：进入步骤时 `genre` 已确定（窗9 后 B1→B2 链路）；勿在 B2 用错 genre
   - B4 提交失败时阻止进入 B5（勿 silent local_only 过关，除非 API 明确降级且 log）

4. kiosk/edu/edu-styles.css（推荐）
   - `.skill-chip` · `.skill-chip.selected` · 触控 ≥48px
   - `.chip-row` 横向滚动（与 B1 示例芯片一致）

## 必读
- config/creative_templates/shmup.json（skill_pick 范本 · q_skill optional）
- config/creative_templates/fighting.json（**无 q_skill** · 仅 4 题 single_choice）
- config/creative_templates/platformer.json · racing.json（对照 5 题结构）
- config/optional_skills.json（skill id → 中文 label）
- backend/app/routers/creative.py · `_validate_answers` skill_pick 分支
- 05-工具脚本/e2e_b_edu_batch.py · `SLUG_CASES`（answers 键名与取值勿自造）
- kiosk/edu/steps/b4-creative.js · b2-name.js · edu-wizard.js（现状）

## B2 各款 name_suggestions 验收原文（API 应返回）
| genre | 期望首条芯片（≥4 条全来自 API） |
|-------|--------------------------------|
| shmup | 雷霆小队 |
| survivor | 糖果幸存者 |
| pingpong | 弹弹乐 |
| fighting | 像素拳王 |
| parkour | 无尽奔跑 |
| racing | 欢乐赛车 |
| platformer | 星星大冒险 |

## B4 手工验收（backend :8000 + python -m http.server 8080 → /kiosk/edu/）
对每款走 B1（点对应 example_chip）→ B2 → B4（可不 generate）：
| 检查项 | shmup | fighting | racing |
|--------|-------|----------|--------|
| B4 题数 | 4（3 单选 + 可选技能） | **4**（全 single_choice） | 4（3 单选 + 可选技能） |
| skill_pick | bomb / laser_beam 可多选 | N/A | boost 等 |
| B2 芯片 | 含「雷霆小队」 | 含「像素拳王」 | 含「欢乐赛车」 |

**最低**：shmup + fighting + racing 三款全过；**建议**：七款各点一遍 B1 芯片。

## 本窗可做浏览器 smoke（p1-edu-frontend 要求）
- 改 kiosk/edu 后：至少 1 款 B1→B2→B4 点通（建议 shmup）
- 完整七款浏览器冒烟归窗13

## 硬禁止
- backend/ · config/creative_templates/** · config/intent_genre_lexicon.json
- templates/** · `kiosk/edu/steps/b1-intent.js`（窗9 已交付，勿改逻辑）
- `code-highlight.js` / B7 POST 联调（窗13 或后续）
- 改窗1–6 四文件（creative · anchors · theater · hooks）

## 收工报告
- widget 支持表（single_choice / skill_pick × 七款题数）
- 三款逐步记录：B2 芯片文案 + B4 题数 + skill 提交 payload 样例
- fighting「无 skill_pick」确认
- 声明未改 backend/templates/core
```

---

## A.11 窗11 · P1-08 POST play/action

**模型**：Composer  
**前置**：无（可与窗9 并行）

```text
【6.24 P1-R · 窗11 · P1-08 play/action · 完整施工单 v1.0】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：backend play 路由 + 测试（本窗不改 kiosk · 不改 templates/core）

## 交付
1. backend/app/routers/play.py
   - 新增 POST /sessions/{session_id}/play/action
   - Request body: { "action_id": "jump" }（Pydantic 强类型）
   - 校验：session 存在 · workspace_path 有效 · action_id 非空
   - 行为：append workspace/{id}/.edu_actions.jsonl
     行格式：{"action_id":"...","t_ms":<unix_ms>}（与 edu_action_bridge.gd 一致）
   - Response: { "ok": true, "action_id": "...", "t_ms": ... }

2. backend/app/services/edu_workspace.py 或独立 helper
   - append_edu_action(workspace_root, action_id) 复用 read 侧路径逻辑

3. backend/tests/test_play_action.py（新建，推荐）
   - 创建 session → generate/v2 platformer → POST action → GET actions 含该 id

## 必读
- backend/app/routers/play.py（GET actions 现实现）
- backend/app/services/edu_workspace.py → read_edu_actions
- templates/_edu/edu_action_bridge.gd（jsonl 格式）
- 开发文档/模板引擎/6.24_B7_操作代码联动功能需求说明_v1.0.md §5

## 验证
pytest backend/tests/test_play_action.py -q
或 curl：
  POST .../play/action {"action_id":"jump"}
  GET .../play/actions?since=0 → actions 非空

## 硬禁止
- templates/{slug}/core/**
- 本窗不要求 Godot HTTP 上报（文件桥仍有效）
- 不改 generate/v2 注入逻辑

## 收工报告
- API 路径与样例 JSON
- pytest 输出
- 与 GET /play/actions 轮询兼容性说明
```

---

## A.12 窗12 · P1-09 Kiosk A/B/edu 分流

**模型**：Auto 或 Composer  
**前置**：窗10 ✅（edu B1–B4 API 层可用）· 窗9/11 ✅ · **浏览器七款冒烟归窗13**

```text
【6.24 P1-R · 窗12 · P1-09 Kiosk 分流 · 完整施工单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：kiosk/ **入口层 only**（index.html · wizard.js 顶部 · styles.css 小改）· 可选 edu/index.html 返回链 · config 说明字段

## 前置上下文（勿重复施工）
- 窗10 已交付：`kiosk/edu/steps/b4-creative.js` · `b2-name.js` · `edu-wizard.js` · `edu-styles.css`
- `/kiosk/edu/` 直达已可用（B0–B7 完整）；本窗只补 **A 链入口 → edu 分流**
- 浏览器 B1→B2→B4 点通冒烟 **不在本窗必做**（窗13 七款 + `?mode=edu` 可一并验）
- 总控回归：`validate` 7/7 · `e2e_b_edu_batch` 6/6 仍须 PASS

## 现状缺口（必读再改）
- `kiosk/wizard.js`：`initApp()` 直接启动 A 链，**无** `URLSearchParams` 检测 `mode=edu`
- `kiosk/index.html`：footer 仅有静态 `<a href="edu/">`；header 无模式标签
- `kiosk/edu/index.html`：无返回 A 链「展厅快玩」入口（可选交付）
- `6.24_技术与执行规范` §三：`/kiosk/?mode=edu` 应等价进入教育 B 链主路径

## 交付
1. kiosk/wizard.js（推荐：文件最顶部 IIFE 内、**initApp 之前**）
   - 解析 `location.search`：`mode=edu`（大小写不敏感）→ **立即跳转** `edu/`（相对路径）
   - 跳转方式：`location.replace('edu/' + location.hash)` 或等价，避免回退仍进 A 链
   - **禁止**先跑 `loadBootstrap()` / `renderStepForm()` 再跳（减少闪屏与多余 session）
   - 默认无 `mode` 或 `mode≠edu`：行为与现网完全一致（shortcut 七宫格 · S9）
   - footer `btnFullWizard`「完整创作向导 S0–S9」逻辑 **不得破坏**

2. kiosk/index.html（小改，可选）
   - header 加模式标签，如 `<span class="mode-badge">展厅快玩</span>`（纯 UI，不影响逻辑）
   - footer「🎨 创作工坊」链接保持；可改为 `href="edu/"` 或 `?mode=edu` 二选一（须与 §验证一致）

3. kiosk/edu/index.html + edu-styles.css（推荐）
   - header 或页脚加「← 展厅快玩」→ `../`（`/kiosk/`）
   - 触控目标 ≥48px；勿改 B0–B7 步骤脚本

4. kiosk/styles.css（可选）
   - `.mode-badge` 样式（若 index.html 加了标签）

5. config/kiosk_ui_spec.json（可选 · 仅文档字段）
   - 增补 `entry_modes`: `{ "a_chain": "/kiosk/", "b_legacy": "/kiosk/ + btnFullWizard", "edu": "/kiosk/edu/ | /kiosk/?mode=edu" }`

## 必读
- kiosk/index.html · wizard.js（现状 · 尤其 `initApp` · `btnFullWizard` · `SHORTCUT_FLOW`）
- kiosk/edu/index.html（返回链落点）
- 开发文档/模板引擎/6.24_技术与执行规范_v1.0.md §三路由
- 开发文档/模板引擎/6.24_P1-R_前端与联调收工手册_v1.0.md §P1-09

## 验证（python -m http.server 8080 · backend :8000 可选）
| URL | 期望行为 |
|-----|----------|
| `http://127.0.0.1:8080/kiosk/` | A 链 shortcut：S0→S1 七宫格→S9，无跳 edu |
| `http://127.0.0.1:8080/kiosk/?mode=edu` | **进入** `/kiosk/edu/`（B0 欢迎或 B1，无 A 链闪屏） |
| `http://127.0.0.1:8080/kiosk/edu/` | 教育工坊正常；B1 七芯片可见（窗9） |
| `http://127.0.0.1:8080/kiosk/` + 点「完整创作向导」 | 仍进 S0–S9 full flow |
| edu 页点「展厅快玩」（若做了返回链） | 回 `/kiosk/` A 链 |

**最低**：上表前三行 + full wizard 一行；**建议**：edu 返回链 + `?mode=EDU` 大小写各测一次。

## 硬禁止
- backend/ · templates/** · `templates/{slug}/core/**`
- `kiosk/edu/steps/**` · `kiosk/edu/edu-wizard.js` 业务逻辑 · `b1/b2/b4` · `code-highlight.js`
- 删除或弱化 A 链七宫格 / shortcut / S9 launch
- 为过关改 `e2e_b_edu_batch` 或 creative 配置

## 收工报告
- 五种 URL 行为表（实测结果）
- 改动文件列表
- `?mode=edu` 实现方式（replace vs href · 检测位置行号）
- 声明未改 edu 步骤脚本与 backend
```

---

## A.13 窗13 · P1-R 收工（冒烟 + 清单回写）

**模型**：Composer  
**前置**：窗9–12 ✅（含 `?mode=edu` 分流 · B4/B2 通用化）

```text
【6.24 P1-R · 窗13 · 收工 · 完整施工单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
前置：P1-04~06/08~09 已交付 · backend :8000 常驻 · `python -m http.server 8080`

## 前置上下文（窗9–12 已交付 · 勿重复施工）
- 窗9：七款 intent match + B1 七芯片
- 窗10：B4 skill_pick + B2 API（fighting **无** q_skill；其余 3 单选 + 1 可选技能 = **4 题**）
- 窗11：POST `/play/action` · pytest 4/4
- 窗12：`/kiosk/?mode=edu` → `location.replace('edu/')` · edu「← 展厅快玩」返回链
- 窗10/12 **未做**深测项：B1 七芯片点通 · B2/B4 浏览器七款 → **本窗必补**

## 本窗目标（P1-R 手册 §四 #6–#8）
| # | 标准 | 本窗交付 |
|---|------|----------|
| 6 | 浏览器或脚本冒烟 ≥3 款（建议 7 款） | smoke 脚本 + 实测记录 |
| 7 | `6.24_P1_六款并行施工手册` §六 **6/6** | 回写 §5 为 ✅ |
| 8 | 整合清单 P1 **12/12** | 文档回写 |

## 交付
### 1. 冒烟脚本（二选一或组合，推荐 A+B）
**A. `05-工具脚本/e2e_b_edu_browser_smoke.py`（新建 · 推荐）**
- **import 复用** `e2e_b_edu_batch.SLUG_CASES`（勿复制粘贴 answers）
- 覆盖六款 + **platformer**（七款完整）：
  - 六款：batch `SLUG_CASES` 的 `intent_text` · `display_name` · `creative_answers`
  - platformer：intent「我想玩马里奥闯关」· name「星星大冒险」· answers 见附录 A 附表
- **实现路径（择一，须在报告中说明）**：
  - **Playwright（推荐）**：打开 `http://127.0.0.1:8080/kiosk/edu/`（或首条用 `?mode=edu` 验窗12）
    · B0 等待 bootstrap → B1 点 example_chip 或填 intent → B2 点 name 芯片 → B4 点 option/skill → 下一步至 generate 成功
    · 断言：`#workName` 含作品名 · 或 log/API 侧 `generate/v2` ok（可用 `page.on('request')` 监听）
    · fighting：仅 4 道 single_choice，**不点** skill_chip
  - **HTTP 薄封装（降级）**：若环境无 Playwright，脚本走与 batch 相同 API 链，但 **必须**附手工浏览器 checklist（≥3 款截图或逐步记录）否则不算过关 #6
- 不启动 Godot 窗口（generate 后 HTTP 断言即可，MCP 仍归 E2E-001/窗8）
- CLI：`python 05-工具脚本/e2e_b_edu_browser_smoke.py [--kiosk http://127.0.0.1:8080] [--api http://127.0.0.1:8000] [--slugs shmup,fighting,racing]`

**B. 手工验收表（最低兜底 · 若 A 为 HTTP-only）**
- 至少 **shmup + fighting + racing** 三款浏览器点通 B1→B2→B4→generate
- 附：B2 首条芯片文案 · B4 题数 · fighting 无 skill 确认

### 2. `config/l1_e2e_acceptance.json`
- 新增 case `E2E-B-EDU-BROWSER`（或扩 BATCH 注记 browser 路径）
- 关联脚本路径 · slugs 列表 · preconditions（backend + :8080）

### 3. `开发文档/模板引擎/评审记录/E2E-B-EDU-P1-R_收工.md`（新建）
- P1 12/12 摘要 · 回归命令输出 · 冒烟 3/7 或 7/7 · 遗留 E17/P2

### 4. 文档回写（本窗必做）
| 文档 | 动作 |
|------|------|
| `6.24_整合任务清单_v1.0.md` | P1-04~09 全 ✅ · 收工行 ✅ · 进度 12/12 |
| `工作状态记录_v1.0.md` §6.24 | P1 **12/12** · 窗9–13 表全 ✅ |
| `6.24_P1_六款并行施工手册_v1.0.md` §六 | #5「kiosk/edu 换意图进 B4」→ ✅ · 总评 6/6 |
| `6.24_文件映射表_v1.0.md` | browser 脚本 · play/action POST · entry_modes |
| `6.24_P1-R_总控对接与启动_v1.0.md` §二仪表盘 | P1-R 窗9–13 全 ✅（可选） |

## 七款向量（与 batch 对齐 · 勿自造 id）
| slug | B1 芯片/句 | B2 首芯片 | B4 要点 |
|------|------------|-----------|---------|
| platformer | 马里奥闯关 / 我想玩马里奥闯关 | 星星大冒险 | q_skill:["double_jump"] |
| shmup | 我想打飞机 | 雷霆小队 | q_skill:["bomb"] |
| survivor | 割草打怪 | 糖果幸存者 | q_skill:["magnet"] |
| pingpong | 乒乓球 | 弹弹乐 | q_skill:["curve_ball"] |
| fighting | 格斗双人 | 像素拳王 | **无 skill** · 4×single_choice |
| parkour | 跑酷 | 无尽奔跑 | q_skill:["slide"] |
| racing | 赛车 | 欢乐赛车 | q_skill:["boost"] |

## 环境与前检
```powershell
.\05-工具脚本\run_backend.ps1          # :8000 常驻，勿 TestClient
python -m http.server 8080             # 另开终端
# Session 池满：GET /sessions → DELETE 各 id（避免 429）
pip install playwright && playwright install chromium  # 若走 Playwright 路径
```

## 验证（总控将复跑）
python 05-工具脚本/validate_creative_templates.py          → OK: 7
python 05-工具脚本/e2e_b_edu_batch.py http://127.0.0.1:8000 → 6/6（回归，不可回退）
python 05-工具脚本/e2e_b_edu_browser_smoke.py               → ≥3/7 PASS（建议 7/7）
python -m pytest backend/tests/test_play_action.py -q      → 4/4（回归，可选）
# platformer 深测已由 E2E-B-EDU-001 覆盖，本窗 smoke 含 platformer 即七款齐

## 硬禁止
- `templates/{slug}/core/**`
- 为过关改 creative 数值 / 弱化 batch 断言
- 本窗勿大改 kiosk/edu 业务逻辑（仅 smoke 所需 `data-testid` 可选小改，须报告）

## 收工报告
- P1 12/12 勾选列表
- P1-R 手册 §四 8 条逐条状态
- 施工手册 §六 6/6 截图或表
- smoke 实现方式（Playwright vs HTTP+手工）与 pass 计数
- 遗留：E17 人工 · P2 LLM · B7 POST 前端去抖
- git 建议 add 列表（不自动 commit 除非用户要求）
```

---

## 附：七款 B4 answers 速查（窗10/13 用）

照 `05-工具脚本/e2e_b_edu_batch.py` · `SLUG_CASES` · 勿自造 option id。

| slug | intent | answers 要点 |
|------|--------|--------------|
| platformer | 我想玩马里奥闯关 | q_move:fast · q_jump:high · q_enemy:hard · q_skill:["double_jump"] |
| shmup | 我想打飞机 | q_speed:fast · q_fire_rate:dense · q_spawn:dense · q_skill:["bomb"] |
| survivor | 割草打怪 | q_speed:fast · q_spawn:dense · q_duration:intense · q_skill:["magnet"] |
| pingpong | 乒乓球 | q_ball:fast · q_win:long · q_ai:strong · q_skill:["curve_ball"] |
| fighting | 格斗双人 | q_speed:fast · q_light:strong · q_ai:strong · q_heavy:strong |
| parkour | 跑酷 | q_run:fast · q_jump:high · q_obstacle:dense · q_skill:["slide"] |
| racing | 赛车 | q_speed:fast · q_turn:nimble · q_lap:long · q_skill:["boost"] |

---

*v1.0 · 2026-06-24 · P1-R 窗9–13 · 接 P1 附录 A 窗8 之后*
