# 6.24 展陈 P0 · 附录 A · 完整窗口提示词 v1.0

> **主文档**：[`6.24_展陈P0_执行手册_v1.0.md`](./6.24_展陈P0_执行手册_v1.0.md)  
> **前置快照**：[`快照/6.24_展陈P0_开工前状态快照_v1.0.md`](./快照/6.24_展陈P0_开工前状态快照_v1.0.md)  
> **用法**：新开 Cursor 窗口 → 选推荐模型 → **复制对应章节整段** 作为首条消息  
> **约束**：遵守 [`.cursor/rules/exhibition-p0.mdc`](../../.cursor/rules/exhibition-p0.mdc)

---

## 模型速查

| 窗 | 任务 | 推荐模型 | 可并行 |
|----|------|----------|--------|
| 14 | E-P0-19 隔离 | **Composer** | ✅ 2026-06-24 |
| 15 | E-P0-17 真代码 | **Composer** | ✅ 2026-06-24 |
| 16 | E-P0-18 B7试玩 | **Sonnet 思考** 或 **Composer** | ✅ 2026-06-24 |
| 17 | D6 七款宫格 | **Auto** 或 **Composer** | ✅ 2026-06-24 · 技术收工 |
| 18 | E17 人工签字 | **人工** + Composer 填表 | ✅ 2026-06-24 · 操作员确认 |
| 19 | 彩排收工 | **Composer** | ← **当前** · 等窗19 回归 |

---

## A.14 窗14 · E-P0-19 鲁棒性 · 启停复位 · 隔离

**模型**：Composer  
**前置**：P1-R 收工 · `workspace_guard` 已有

```text
【6.24 展陈 P0 · 窗14 · E-P0-19 · 完整施工单 v1.0】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：bootstrap/release/隔离脚本 · 可改 kiosk/session.js · wizard.js btnReset · 禁止改 templates/core

## 交付
1. 审计并加固 bootstrap 孤儿 workspace 清理
   - backend/app/routers/bootstrap.py（或等价入口）
   - 启动时删除无活跃 session 的 workspace 目录

2. 审计 release 全路径
   - edu：kiosk/edu/session.js sendBeacon + POST /release
   - A 链：kiosk/wizard.js pagehide/beforeunload 是否 release
   - 后端 DELETE /sessions/{id} 删 workspace

3. 「重新开始」复位
   - edu-wizard.js btnReset：清 session 状态 · 回 B0 · release 旧 id
   - wizard.js A 链 btnReset 同等行为

4. templates 零写入守卫
   - 确认 generate 只写 workspace/{id}/
   - 文档或脚本：展前 git diff templates/ 应为空

5. 新建 05-工具脚本/e2e_exhibition_isolation.py
   - 顺序创建 3 个 session
   - 用户 A：platformer generate + 独特 display_name
   - release A
   - 用户 B：新 session 不得读到 A 的 meta/display_name/tuning
   - 断言 workspace 路径隔离

6. 可选：config/l1_e2e_acceptance.json 登记 E2E-EXHIBITION-ISOLATION

## 必读
- backend/app/services/workspace_guard.py
- backend/app/routers/sessions.py
- kiosk/edu/session.js
- kiosk/wizard.js（release/reset 段）
- 开发文档/模板引擎/6.24_展陈P0_需求规格_v1.0.md §E-P0-19

## 验证
.\05-工具脚本\run_backend.ps1
python 05-工具脚本\e2e_exhibition_isolation.py http://127.0.0.1:8000
python 05-工具脚本\e2e_b_edu_batch.py http://127.0.0.1:8000  # 不退化

## 硬禁止
- templates/{slug}/core/**
- 改 creative_templates 数值过关

## 收工报告
- 改动文件列表
- isolation 脚本输出
- A/edu release 行为说明
- 遗留风险（若有）
```

---

## A.15 窗15 · E-P0-17 B6/B7 左侧真代码

**模型**：Composer  
**前置**：可与窗14并行

```text
【6.24 展陈 P0 · 窗15 · E-P0-17 · 完整施工单 v1.0】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：backend 只读 config API + kiosk/edu 代码展示 · 禁止改 templates/core

## 交付
1. 新增只读 API（建议路径，可等价实现）
   GET /sessions/{session_id}/workspace/game-config
   → { "ok": true, "genre": "...", "content": "<完整 JSON 文本>", "path": "..." }
   - 经 workspace_guard 校验路径
   - 无文件时 404 + 明确错误

2. kiosk/edu/edu-wizard.js
   - generate/v2 成功后 fetch 真 config
   - renderB6Ready / renderB7Play 左侧用真文本
   - 有 workspace 时禁止 getFallbackConfigSnippet() 冒充真文件
   - 无 workspace：面板显示「请先完成制作」（可保留 fallback 仅用于 dev 且须 console.warn）

3. kiosk/edu/code-viewer.js（若需）
   - setContent 支持完整 JSON 字符串 + highlightLines

4. 浏览器 smoke：platformer 走完 B5 后左侧含用户 display_name / tuning 真字段

5. 可选：backend/tests/test_workspace_game_config.py

## 必读
- kiosk/edu/edu-wizard.js（getFallbackConfigSnippet, renderB6Ready, renderB7Play）
- backend/app/routers/generate.py（config 路径）
- 开发文档/模板引擎/6.24_展陈P0_文件映射表_v1.0.md

## 验证
python -m pytest backend/tests/ -q -k "workspace or game_config"  # 若有新测
python 05-工具脚本\e2e_b_edu_browser_smoke.py --kiosk http://127.0.0.1:8080 --api http://127.0.0.1:8000

## 硬禁止
- templates/{slug}/core/**
- 窗16 的 launch UI（本窗只做左侧真代码）

## 收工报告
- API 契约与示例 curl
- B6 截图或 smoke 日志
- fallback 降级策略说明
```

---

## A.16 窗16 · E-P0-18 B7 试玩展陈联调

**模型**：Sonnet 思考 或 Composer  
**前置**：窗15 ✅（`fetchWorkspaceConfig` · `applyCodeViewerContent` · game-config API 已冻结）

```text
【6.24 展陈 P0 · 窗16 · E-P0-18 · 完整施工单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：kiosk/edu **B6/B7 右侧试玩区 + launch 状态 + 轮询时机** · 可只读 play.py · 禁止改 templates/core

## 窗15 已冻结（本窗勿动）
- `GET /sessions/{id}/workspace/game-config` 及 `backend/app/routers/sessions.py` 中相关 endpoint
- `fetchWorkspaceConfig()` · `applyCodeViewerContent()` · `workspaceConfigContent` 缓存逻辑
- B2 完成时 `POST /sessions/{id}/wizard/S0` 写入 `display_name`
- `05-工具脚本/e2e_b_edu_browser_smoke.py` 中 `workspace_game_config` 断言逻辑

## 已知代码现状（必读后再改）
- B6：`renderB6Ready()` 已有 `#btnLaunch` + `#launchStatus` · `launchGame()` 已 POST `/play/launch`
- B7：`renderB7Play()` 右栏仍为 `godot-placeholder`「embed contain」文案 — **须替换**
- B7：`renderB7Play()` **一进入就** `startPolling()` — **应改为 launch 成功后再启**（避免未开 Godot 时空轮询）
- launch 成功响应字段（`PlayLaunchResponse`）：`ok` · `pid` · `project_path` · `godot_path` · `message` · `already_running`

## 交付
1. `launchGame()`（B6 按钮）
   - 成功：`#launchStatus` 显示「Godot 已启动 · 请到游戏窗口试玩」
   - 可折叠 `<details>` 展示 `pid` · `project_path`（儿童向默认折叠）
   - `already_running` 时文案区分「游戏已在运行」
   - 失败：红色可理解文案 · 保留进入 B7 演示路径 + `btnLoadClassic` 降级

2. `renderB7Play()` 右栏（替换 placeholder）
   - 主文案：**外置 Godot 窗口**（非 iframe embed）· 引导讲解员指向大屏旁游戏窗
   - 显示与 B6 一致的 launch 状态区（可抽 `renderLaunchStatusPanel(launchData)` 复用）
   - 模拟按钮保留但须标注 **「讲解员演示用」**（`hint` 或 `aria-label`）
   - 移除或改写「Godot 窗口将在此 embed」误导文案

3. 轮询时机
   - `EduCodeHighlight.startPolling(sessionId)` 仅在 **launch 成功** 或用户从 B6 成功 launch 进入 B7 后调用
   - 进入 B7 若未 launch：显示「请先点击开始试玩」· 不启动轮询
   - 可选：`GET /sessions/{id}/play/status` 每 3s 刷新 running/stopped（仅 UI，非必须）

4. `dual-pane.js`（仅布局/样式微调）
   - `.launch-status.ok` / `.launch-status.err` 展厅可读字号
   - 右栏 `.godot-frame-wrap` 改为「状态卡片」而非假 embed 框

5. 不实现浏览器内嵌 Godot 引擎

## 必读
- kiosk/edu/edu-wizard.js — `launchGame`（约 L390）· `renderB6Ready` · `renderB7Play`（约 L418）
- backend/app/routers/play.py — `PlayLaunchResponse` · `launch_play` · 可选 `play/status`
- kiosk/edu/code-highlight.js — `startPolling` · `stopPolling`（若有）
- 开发文档/模板引擎/6.24_展陈P0_需求规格_v1.0.md §E-P0-18

## 验证
python -m pytest backend/tests/test_play_action.py -q
python 05-工具脚本\e2e_b_edu_browser_smoke.py --kiosk http://127.0.0.1:8080 --api http://127.0.0.1:8000 --slugs platformer
  # 必须保持 workspace_game_config PASS；可选断言 #launchStatus 或 B7 状态文案
手动：B 链 platformer → B6 点试玩 → 外置 Godot 窗口 → B7 跳/踩 → 左侧高亮

## 硬禁止
- templates/{slug}/core/**
- 改 game-config API · applyCodeViewerContent · fetchWorkspaceConfig（窗15 范围）
- 改 generate/v2 注入逻辑
- 删除模拟按钮（须保留为讲解员降级）
- 用模拟按钮冒充 E17 过关（人工窗18 仍须真 Godot）

## 收工报告
- launch 成功/失败/already_running 三种 UI 说明
- 轮询启动时机变更说明（附 renderB7Play 前后对比）
- E17 人工试玩衔接步骤（B6 launch → B7 实机）
- platformer smoke 日志（workspace_game_config 仍 PASS）
```

---

## A.17 窗17 · D6 Kiosk 精选 7 款宫格联调

**模型**：Auto 或 Composer（**本窗以人工试玩为主**，代码改动应极小）  
**前置**：窗14 ✅（A 链 `release` / `beforeunload` / `btnReset` 已加固）· 可与窗16 并行 · **禁止动 edu/**

```text
【6.24 展陈 P0 · 窗17 · D6 · 完整施工单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：A 链 **人工验收 + 试玩记录表** · 仅当阻塞时改 `kiosk/wizard.js` / `kiosk/index.html` · 禁止改 templates/core · 禁止改 kiosk/edu/

## 窗14/15/16 已冻结（本窗勿动）
- `kiosk/edu/**` 全部（属窗15/16）
- `backend/app/routers/sessions.py` game-config endpoint
- `templates/{slug}/core/**`
- 窗14 已合入的 A 链 release 逻辑（`beforeunload` · `sendBeacon` · `btnReset` → `releaseAsync`）— 除非 D6 试玩发现明确回归

## 任务性质
D6 **不是**再开发 7 款游戏 · 是确认 **A 链快玩** 在展厅路径下 7 款均可点开、外置 Godot 可玩 ≥3min、无阻塞 ERROR。
B 链 edu 7/7 已由 P1-R 证明 — **本窗不重复测 edu**。

## 已知代码现状（验收前必读）
- 入口：`http://127.0.0.1:8080/kiosk/`（非 `/kiosk/edu/`）
- shortcut 流程：`S0`（可跳过起名）→ `S1` 宫格 → **直跳** `S9` 试玩（`SHORTCUT_FLOW = ["S0","S1","S9"]`）
- S1 宫格：`loadBootstrap()` 读 `kiosk_ui_spec.featured_genres.slugs` 过滤 `/genres` — **应恰好 7 张卡**
- S9 启动：`launchGodotPlay()` → `POST /sessions/{id}/play/launch` → **外置 Godot 窗口**（经典版 `templates/{slug}/`，非 workspace 个性化）
- 页脚「完整创作向导」走 `full` 流（S0–S9 个性化）— D6 **主验 shortcut**，full 仅抽 1 款 smoke 即可
- `?mode=edu` 分流（窗12）须保持有效 — 本窗改 wizard 时勿破坏

## 交付
1. **S1 宫格核对**（代码或目视）
   - 7 slug 与 `config/miaoda_parity_r2_registry.json` 中 `r2_done` 一致：
     platformer · shmup · survivor · pingpong · fighting · parkour · racing
   - 副标题与 `featured_genres.subtitle` 一致（「共 7 款精选游戏」）
   - **非目标**：11 L0 全扫 · tower_defense 等 C 流水线品类

2. **七款人工试玩**（主交付）
   - 每款：shortcut S0（可跳过）→ S1 选款 → S9「▶ 启动试玩」→ Godot 窗口
   - 实玩 **≥3 分钟** · 记录可玩性/卡顿/崩溃
   - 填写：`开发文档/模板引擎/6.24_展陈P0_D6_七款试玩记录表_v1.0.md`（7/7 勾选）

3. **阻塞性修复**（仅当试玩失败时）
   - 允许改：`kiosk/wizard.js` · `kiosk/index.html` · `kiosk/styles.css`（A 链 UI）
   - 允许读：`config/kiosk_ui_spec.json`（不改 featured 列表除非与 registry 不一致）
   - 禁止为过关改 `templates/{slug}/core/**` 或 creative 数值

4. **可选自动化抽检**
   - `godot-mcp run_project` 对失败品类复现 ERROR
   - 不要求为本窗新建 E2E 脚本

## 必读
- kiosk/wizard.js — `loadBootstrap` · `renderS1` · `renderS9` · `launchGodotPlay` · `btnReset`
- config/kiosk_ui_spec.json — `featured_genres`
- config/miaoda_parity_r2_registry.json — 七款 `r2_done`
- 开发文档/模板引擎/6.23_七天交工工作清单_v1.0.md D6 节
- 开发文档/模板引擎/6.24_展陈P0_需求规格_v1.0.md §D6

## 环境与验证
.\05-工具脚本\run_backend.ps1          # :8000
python -m http.server 8080             # Kiosk
# 人工 7 款试玩表 7/7

python 05-工具脚本\validate_creative_templates.py    # 7/7 不退化
python 05-工具脚本\e2e_exhibition_isolation.py http://127.0.0.1:8000   # 若改了 wizard.js 必跑
python 05-工具脚本\e2e_b_edu_batch.py http://127.0.0.1:8000           # 可选 · 确认未误伤 B 链后端

## 每款试玩检查项（记录表备注栏）
| slug | 关注项 |
|------|--------|
| platformer | 跳跃/踩怪 · 关卡节奏 |
| shmup | 射击/Boss/道具 |
| survivor | 割草密度 · 升级 |
| pingpong | 双人对战/人机 · 球拍手感 |
| fighting | 键盘双人 · 大招 |
| parkour | 障碍密度 · 坠落复位 |
| racing | 转向/加速 · 圈数 |

## 硬禁止
- 11 L0 纳入 D6 验收范围
- templates/{slug}/core/**
- kiosk/edu/**（窗15/16）
- 将 D6 与 E17 混测（E17 走 edu B 链 · 窗18）
- D7 Windows exe（P1）

## 收工报告
- 试玩记录表 7/7 填写（或截图）
- S1 宫格截图（可见 7 张卡 + 副标题）
- 每款 launch 成功/失败摘要
- wizard.js 改动说明（若无改动写「无代码变更，纯人工验收」）
- 非 P0 问题记入 backlog 表（可选）
```

---

## A.18 窗18 · E17 人工展厅试玩 platformer

**模型**：**人工主导**（展厅触控 + 外置 Godot）· Composer 仅协助填表/排错  
**前置**：窗16 ✅ · 窗15 ✅ · **与 D6 无关**（D6 走 A 链 `/kiosk/` · E17 走 B 链 `/kiosk/edu/`）

```text
【6.24 展陈 P0 · 窗18 · E17 · 人工验收单 v1.1】

仓库：E:\文三路AI馆\2.ai生成游戏
本窗 **100% 人工实机** · Agent 不代过关 · 仅协助日志/填表/非 core 排错

## 与 D6 / 窗16 边界（必读）
| 项 | D6 窗17（已完成） | E17 窗18（本窗） |
|----|------------------|------------------|
| 入口 | `/kiosk/` shortcut | `/kiosk/edu/` 全链 B0–B7 |
| 游戏来源 | `templates/{slug}/` 经典版 | `workspace/{id}/` 用户生成版 |
| 左侧代码 | 无 / 非本项 | 真 `game_config.json`（窗15） |
| 过关标准 | launch + ≥3min 可玩 | **真 Godot** 跳/踩怪 → **左侧高亮** ≥120s |
| 模拟按钮 | A 链无 | B7「讲解员演示用」**禁止**用于签字过关 |

## 窗16 已就绪（验收时核对，勿回改除非阻塞）
- B6「开始试玩」→ `POST /play/launch` → 成功自动进 B7
- B7 右栏：`renderLaunchStatusPanel()` 外置窗口引导（非 embed）
- 仅 `launchState.ok` 后 `EduCodeHighlight.startPolling`
- 失败留 B6 红字；演示模式进 B7 无轮询 — **E17 须走成功 launch 路径**

## 执行步骤（讲解员/开发 · 按序打勾）
1. `.\05-工具脚本\run_backend.ps1` + `python -m http.server 8080`
2. 打开 `http://127.0.0.1:8080/kiosk/edu/`（或 `/kiosk/?mode=edu`）
3. B1 意图选 **platformer**（例：「我想玩马里奥闯关」）→ B2 起名 → B4 完型 → B5 制作
4. **B6** 确认左侧 JSON 含用户 `display_name` / `tuning`（非 fallback）
5. B6 点 **「开始试玩」**（非「进入试玩」演示）→ 外置 **Godot 窗口** 弹出
6. 自动进 **B7**：右侧 launch 状态卡 + 左侧真代码
7. 在 **Godot 窗口**（非网页）操作：
   - 跳跃 ≥1 次 → 左侧 `jump` 锚点高亮
   - 踩怪 ≥1 次 → 左侧 `stomp_enemy` 锚点高亮
8. 连续试玩 **≥120s**（记起止时间）
9. 填写 `开发文档/模板引擎/评审记录/E17_展厅试玩_platformer.md`

## 证据要求（收工报告）
- [ ] B6 或 B7 **双栏截图** 1 张（可见真 config + launch 状态）
- [ ] 高亮证据 1 份：`GET /sessions/{id}/play/actions` 返回含 jump/stomp · 或 `.edu_actions.jsonl` 片段
- [ ] 签字表全勾 · 起止时间 ≥120s

## Agent 可协助（不替代人工操作）
- 读 `workspace/{id}/.edu_actions.jsonl` · curl `GET .../play/actions`
- 排查：launch 503 · 轮询未启 · 锚点行号与真 config 不一致
- 微调：`code-highlight.js` 轮询间隔 · **禁止**改 `templates/platformer/core/**`
- 若 launch 失败：查 `GODOT_PATH` · `play/status` · workspace 路径

## 硬禁止
- 用 B7「跳！」「踩怪」**模拟按钮**冒充过关（签字表须注明若降级演示）
- 走 A 链 `/kiosk/` shortcut 冒充 E17
- 改窗15 game-config / 窗16 launch 主流程（除非 E17 明确阻塞且范围最小）
- 改 `templates/{slug}/core/**`

## 过关判定
- `E17_展厅试玩_platformer.md` 技术+产品签字 ☑
- 真 Godot 操作触发高亮（非 simulateAction）

## 本窗不收工项（留窗19）
- browser smoke 7/7 · batch 6/6 全量回归
- D6 S1 宫格截图 · 6 款触控 ≥3min 人工补验（窗17 代理验收）
```

---

## A.19 窗19 · 彩排 + 展陈 P0 收工

**模型**：Composer  
**前置**：窗14–18 完成或仅剩 E17 签字

```text
【6.24 展陈 P0 · 窗19 · 彩排收工 · 完整施工单 v1.0】

仓库：E:\文三路AI馆\2.ai生成游戏
会话范围：文档回写 · 全量回归 · 彩排清单

## 交付
1. 跑全量回归（全部 PASS）
   validate 7/7
   e2e_b_edu_batch 6/6
   e2e_b_edu_browser_smoke 7/7
   pytest test_play_action
   e2e_exhibition_isolation

2. 勾选 6.24_展陈P0_彩排清单_v1.0.md
   - A 链 1 款快玩 demo
   - B 链 platformer 全链
   - B 链 +1 非 platformer（建议 fighting）

3. 回写文档
   - 6.24_整合任务清单_v1.0.md 展陈 P0 全 ✅
   - 工作状态记录_v1.0.md §八 足迹
   - 新建/完成 评审记录/6.24_展陈P0_收工.md
   - 快照/6.24_展陈P0_收工后状态快照_v1.0.md（若全部完成）

4. 确认 templates/ 零 diff

## 硬禁止
- 为签字改 templates/core
- 未经用户要求 git commit

## 收工报告
- 回归命令输出摘要
- 彩排清单勾选状态
- 展陈 P0 6/6 过关表
```

---

*v1.3 · 2026-06-24 · 窗16–17 收工 · A.18 v1.1*
