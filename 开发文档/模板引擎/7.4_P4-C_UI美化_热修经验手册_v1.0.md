# 7.4 · P4-C UI 美化 · 热修经验手册 v1.1

> **日期**：2026-07-04 立项 · **2026-07-05** C-1 收工  
> **范围**：`kiosk/edu` B1–B7 沉浸式 UI · 证书 · 排行榜  
> **配套**：[`7.4_P4-C_施工backlog_v1.0.md`](./7.4_P4-C_施工backlog_v1.0.md) · [`7.4_P4-C01_B1泡泡UI_施工足迹与问题记录_v1.0.md`](./7.4_P4-C01_B1泡泡UI_施工足迹与问题记录_v1.0.md)

---

## 一、本手册用途

P4-C 各子窗施工与验收后，在此记录：

- 动效性能陷阱（泡泡/烟花/卡片/GIF）
- RECIPE 边界踩线（误改 `buildRecipeRows` · B4 题面）
- 排行榜关窗检测与 hooks 记分
- 横竖屏主题 token 错位
- **B1 泡泡 + 展厅触控键盘**（§四 · C-1 已验收）

**施工前必读**：[`7.4_P4-C_七款主题与动效映射表_v1.0.md`](./7.4_P4-C_七款主题与动效映射表_v1.0.md)

---

## 二、架构速查（目标态）

```text
B1  EduBubblePicker → b1-intent · EduTouchKeyboard → POST /kiosk/touch-keyboard/{show,hide,warm}
B2  b2-creator → b2-name → ForgeReveal → GenreTheme.apply(slug)
B4  EduB4CardFlow → EduB4Creative（逻辑不变）
B5  EduCodeTheater + EduTheaterOverlays[slug]
B6  EduCertificate（title_template · GIF）
B7  play/status 轮询 → EduLeaderboard
```

---

## 三、经验条目（按窗）

| 窗 | 日期 | 问题 | 根因 | 修复 |
|----|------|------|------|------|
| C-1 | 2026-07-04 | 浏览器见旧 UI | 缓存 | `index.html` `?v=` |
| C-1 | 2026-07-04 | 竖屏无底栏 | 文档流挤出 | toolbar `position:fixed` z-index 50 |
| C-1 | 2026-07-04 | 格斗泡泡 🏓 | 「双人」子串误匹配 | `CHIP_TEXT_GENRE` |
| C-1 | 2026-07-04 | 模拟键盘挡底栏/无拼音 | 非 IME | 删除模拟键 · TabTip API |
| C-1 | 2026-07-04 | 下一步 disabled 卡死 | API 异常无 finally | `goNext` try/finally |
| C-1 | 2026-07-04 | 只能英文 | 未切 zh-CN IME | `show_touch_keyboard.ps1` |
| C-1 | 2026-07-04 | 键盘闪退 | 双触发 Toggle | 仅 pointerdown · ShowIfHidden |
| C-1 | 2026-07-05 | UI 打不开 | 8080 未开 | 双服务 8080+8000 |
| C-1 | 2026-07-05 | 搜狗软键盘 API | 无独立 exe | TabTip + 搜狗默认 IME |
| C-7† | 2026-07-06 | hide 不关闭键盘 | `is_tabtip_visible` 漏检 | `_tabtip_open` + native COM hide |
| C-7† | 2026-07-06 | 新页需点 2–3 次 | dismiss 占 show 冷却 | 零延迟 · 点外/导航即 hide |
| C-7† | 2026-07-06 | 启动 ERR_CONNECTION_REFUSED | 只开 127.0.0.1:80 | `8080/kiosk/edu/` + `launch_workshop.py` |

---

## 四、B1 碰撞泡泡 + 触控键盘（C-1 验收通过 · 2026-07-05）

### 4.1 泡泡实现要点

| 项 | 建议 |
|----|------|
| 渲染 | DOM + `requestAnimationFrame` · 非 Canvas（布局/触控更稳） |
| 图标 | HTML emoji 奖章 · `EduB1Intent.emoji(slug)` · **勿用** preview PNG |
| chip→genre | **`CHIP_TEXT_GENRE` 精确表优先** · 再 fallback `KEYWORD_MAP` |
| 竖屏 | `.step-panel` Grid 居中 · 泡泡层 `bottom:96px` · toolbar fixed |
| 缓存 | 改 JS/CSS 必 bump `index.html` `?v=` |

### 4.2 触控键盘（全 B 链标准 · 2026-07-06 热修收工 `c07-14`）

| 必须 | 禁止 |
|------|------|
| class `edu-touch-input` + `EduTouchKeyboard.bind()` | 页面内自建 QWERTY/常用字面板 |
| **点输入** `pointerdown` → 同步 `focus` + `POST …/show` | `focusin` 重复 Toggle TabTip |
| **点外/泡泡/导航钮** → `blur` + `POST …/hide` | `renderStep` 内 `await dismiss` 阻塞切页 |
| 后端 :8000 · `tabtip_native` 进程内 COM（预热后 &lt;10ms） | 每次 show/hide 起 PowerShell（~2s） |
| `bindNavDismiss` 绑 `#btnNext`/`#btnPrev` pointerdown | 裸开 `http://127.0.0.1`（无 8080 静态服） |
| 入口 `http://127.0.0.1:8080/kiosk/edu/` | 依赖浏览器 focus 单独唤起 TabTip |
| Windows 默认输入法 = 搜狗拼音（或微软拼音） | 假设 TabTip 自动中文 |

**API**：`POST /kiosk/touch-keyboard/show|hide|warm` · hide 靠 `_tabtip_open` 跟踪（勿仅信 HWND 高度检测）。

**搜狗软键盘**：无公开独立 exe；手写备选 `touch_keyboard.provider = "sogou_hand"`。

**详见**：[`7.4_P4-C_触控键盘热修_施工足迹与验收总结_v1.0.md`](./7.4_P4-C_触控键盘热修_施工足迹与验收总结_v1.0.md)

### 4.3 z-index 与 pointer-events

```text
.intent-bubble-field     z-index: 2  · pointer-events: none（子泡泡 auto）
#stepForm / 输入框        z-index: 8
body.edu-step-b1 .toolbar,
body.edu-step-b2 .toolbar  z-index: 50 · pointer-events: auto
body.kiosk-blocked .step-panel .toolbar → 仍 pointer-events: auto
```

### 4.4 展厅双服务

```powershell
python .\05-工具脚本\launch_workshop.py              # 推荐：Redis+API+8080+开浏览器
# 或手动：
python -m http.server 8080 --bind 127.0.0.1   # 项目根 · UI
.\05-工具脚本\run_backend.ps1                  # API + 键盘
```

入口：**http://127.0.0.1:8080/kiosk/edu/**（路径不变 · **勿**只输入 `127.0.0.1`）

### 4.5 后续 C 窗勿破坏

- B2/B4 等新增 `<input>`/`<textarea>` 须接 `edu-touch-input` 标准（见技术规范 §八）
- 勿在 B1 之上叠加 z-index>50 的全屏 fixed 层（除非留底栏命中区）

---

## 五、待后续窗回填

| 窗 | 状态 |
|----|------|
| C-2 名字填空 | ✅ 2026-07-05 · 见 §5.1 |
| C-3 揭幕+主题 | ✅ 2026-07-05 · 见 §5.3–5.4 |
| C-4~C-8 | C-5 ✅ · C-6 🔲 待办验收 · C-7 ✅ 2026-07-06 · 键盘热修 ✅ 2026-07-06 · 见 §5.9–5.13 |

### 5.1 C-2 · B2 名字填空（2026-07-05）

| 项 | 做法 |
|----|------|
| 子屏 | `b2SubStep`: `creator` → `gameName` · 标题/副标题在 `edu-wizard.js` `renderStep` 切换 |
| 文案 | A：「你的名字是？」+ 副标题 · B：主标题「你好，{creatorName}！」· 引导语「让我们制作一个{genreLabel}小游戏吧！」· 标签「你想让它叫什么名字呢？」 |
| 持久化 | 子屏 A 下一步 `PATCH /sessions/{id}` `creator_name` · 失败 **不** 进 B · 子屏 B 仍 `POST wizard/S0` |
| 恢复 | `hydrateSessionMeta()` GET session 填 `creatorName`/`displayName` |
| 顶栏 | B3 起 `updateWorkName()` → `小明《赛车之王》` |
| 竖屏 | `body.edu-step-b2` 复用 B1 网格 + fixed 底栏 z-index 50（勿只 `remove edu-step-b1`） |
| 触控 | 两子屏 input 均 `edu-touch-input` + `EduTouchKeyboard.bind()` |
| 校验 | 空名/非法字符 `showValidationError` · PATCH 400 提示「保存失败，请重试」 |
| 揭幕 | B 子屏完成 → `await EduForgeReveal.play(...)` · 见 §5.3 |
| 版本 | B2 基线 `7.4-c02-03` · 含 C-3 文案 `7.4-c03-03` |

### 5.2 C-2 · 展厅字号（页眉全局 + B2 内容）

**触发**：用户反馈竖屏远距离观看困难。

| 层 | 做法 | 禁止 |
|----|------|------|
| 页眉全局 | `:root` 定义 `--edu-header-brand` / `--edu-header-work-name` / `--edu-header-phase` / `--edu-header-action` 等 | 仅 `edu-step-b2` 写页眉覆盖（各步不一致） |
| 竖屏放大 | `body[data-orientation="portrait"]` 覆写同名变量 · `--edu-header-h: 140px` | 竖屏 B1/B2 再 `display:none` 品牌副标题 |
| B2 内容 | `body.edu-step-b2` 放大 subtitle / input / hint / chip / toolbar | 与页眉变量重复定义同一字号 |
| 高度联动 | `min-height: calc(100dvh - var(--edu-header-h))` 随变量走 | 写死 72px 头高 |

**变量一览**（`edu-styles.css` `:root`）：

```text
--edu-header-brand      品牌主标题
--edu-header-brand-sub  「文三路 AI 教育区 · 看得见的代码」
--edu-header-work-name  【品类】创作中 / 名《游戏名》
--edu-header-phase      创作 · 制作 · 试玩
--edu-header-action     ← 展厅快玩 · 重新开始
--edu-header-h          页眉最小高度（竖屏约 140px）
```

**经验**：后续 C-4~C-8 改字号优先调变量，B0–B7 自动同步。

### 5.3 C-3 · 揭幕过渡（2026-07-05 · 用户验收通过）

| 项 | 做法 |
|----|------|
| 钩子 | `edu-wizard.js` B2 子屏 B 末尾 `await EduForgeReveal?.play({ creatorName, displayName, genre, genreLabel })` |
| 阻塞 | 揭幕期间 `EduWizard.setUiEnabled(false)` · overlay z-index 70 · 5s 超时 skip |
| 时序 | T0 遮罩 400ms → T1 卡片+烟花 500ms → T2 停留 1400ms → T3 卡片淡出 400ms → T4 遮罩淡出 600ms |
| reduced-motion | 跳过烟花 · 卡片 ~0.8s 后直接淡出 |
| 卡片视觉 | 渐变流光边框 · 背景闪烁星 · 品类 emoji · 文字渐变 + 扫光 · `--forge-accent` 读 spec |
| 烟花 | Canvas 多轮连爆 · 星形/圆点粒子 · 拖尾 · 受 `forge_reveal.firework_count` 限制 |
| 脚本顺序 | `genre-theme.js` → `forge-reveal.js` → … → `edu-wizard.js` |
| 版本 | `index.html` `?v=7.4-c03-03` |

### 5.4 C-3 · B2 子屏 B 文案 + UI 背景决策（2026-07-05）

**B2 子屏 B 文案（c03-02）**

| 元素 | 文案 |
|------|------|
| 主标题 `#stepTitle` | 「你好，{creatorName}！」· 副标题 hidden |
| 引导语 | 「让我们制作一个{genreLabel}小游戏吧！」· 无左侧品类图 |
| 输入标签 | 「你想让它叫什么名字呢？」 |

**UI 背景回退（c03-03 · 用户验收决策）**

| 保留 | 移除 |
|------|------|
| `kiosk_edu_spec.genre_themes` 七款 token | `body[data-genre-theme]` 全部 CSS |
| `genre-theme.js` · `themeFor()` 供局部读取 | 揭幕后 `EduGenreTheme.apply()` 切换页面 UI |
| 揭幕卡按品类 accent 着色 | B3–B7 分品类 surface/pattern 背景 |

**经验**：全页 UI 维持 v1.1 **统一蓝白**；品类差异放在**局部**（揭幕卡、B4 widget、B5 剧场 overlay），勿整页换肤除非甲方明确要求。

### 5.5 C-4 · B4 卡片轮播（2026-07-05 · 基线 c04-01）

| 项 | 做法 |
|----|------|
| 呈现 | `EduB4CardFlow.mount` 单题卡 + 顶栏进度 `N / total` · 列表式 fallback 保留 |
| 动画 | 飞出 280ms `translateX(-110%)` · 飞入 320ms `translateX(80%)→0` · 仅 transform/opacity |
| 导航 | `single_choice` 选答自动下一题 · `skill_pick` 卡内「下一题」钮 · dual「上一步」题内回退 |
| 回退 | `edu-wizard.js` `goPrev` 先调 `EduB4CardFlow.prev()` · index=0 才回 B3 |
| widget | `B4_WIDGET_REGISTRY[genre][qid]` + `B4_QUESTION_EMOJI` · accent 读 `themeFor()` |
| 答完提交 | `confirmedIndices` 记录用户点选 · 末题全满才显 `#btnDualNext` |
| 脚本顺序 | `b4-card-flow.js` → `b4-creative.js` → … → `edu-wizard.js` |

### 5.6 C-4 · 验收热修（c04-02 ~ c04-06）

| 版本 | 问题 | 修复 |
|------|------|------|
| c04-02 | 题卡不够「小朋友向」 | 揭幕卡风 gradient shell · 触控字号 `edu-step-b4` |
| c04-03 | B3 字号未同步 · 提交钮过早出现 | `edu-step-dual-create` · `confirmedIndices` · 选答暂停+星星 |
| c04-04 | `[hidden]` 无效 · 选答后锁死 | `.b4-submit-btn[hidden]{display:none!important}` · `pickLocked`/`cardTransitioning` 双锁 |
| c04-05 | 赛车计时环过大 · widget 单一 · B2 IME | SVG 限 40px · 七款 per-q widget · `compositionend`/NFKC |
| c04-06 | 星星不在点击处 | `pointerFromEvent(ev)` · burst 相对 `.b4-card-inner` 定位 |

**经验**：

1. **hidden 按钮**必须加 `display:none!important`，否则被 `.btn { display:inline-flex }` 盖掉。
2. **过渡锁**分「选答暂停」与「切卡动画」两把锁，不可共用一个 `transitioning` 布尔。
3. **SVG widget**必须写死 `width/height`，勿依赖容器撑满。
4. **展厅 IME**：B2 名字要处理 `compositionend` + blur 提交 + 全角转半角。
5. **触控反馈**：星星爆发原点用 `clientX/clientY` 换算到题卡 inner 坐标。

### 5.7 C-5 · B5 剧场 overlay（2026-07-05 · 基线 c05-01 · 验收 c05-08）

| 项 | 做法 |
|----|------|
| 挂载 | `EduCodeViewer.mount()` **之后** `EduTheaterOverlays.start(genre, stageEl)` |
| 舞台 | `#codeWorkspace` 加 `.code-theater-stage` + `data-theater-genre="{slug}"` |
| 注册 | `theater-overlays/index.js` · slug 脚本须先于 index 加载 |
| 背景 | `data-stream.js` 向上流动 token · 代码区 72% 半透明 |
| 分层 | overlay `pointer-events:none` · z-index 6 · 不挡滚动/文件树 |
| accent | `EduGenreTheme.themeFor(slug)` · 禁止整页 `data-genre-theme` |
| stop | `EduTheaterOverlays.stop()` + 移除 stage 类/属性 · 清 timer/WAAPI |

**经验**：overlay 与 `mount()` 同容器时，**后挂载 overlay**；改 theater 相关 JS 须 **全链 bump `?v=`**。

### 5.8 C-5 · 验收热修（c05-02 ~ c05-08）

| 版本 | 问题 | 修复 |
|------|------|------|
| c05-02 | overlay 不可见 | mount 后再 start overlay · `data-edu-phase=build` 字号 |
| c05-03 | 剧场偏素 | `data-stream.js` + 动效放大 + 舞台光晕 |
| c05-04 | 数据流方向 | 改为向上流动 |
| c05-05 | build-wait 图标小且乱 | 200–280px 核心 · 三轨臂公转 · emoji 3.4–4.8rem |
| c05-06 | 图标固定轨迹 | `spawn-utils.js` 边缘随机穿越 · 闯关下栏金币上弹 |
| c05-07 | 跑酷 lane 条纹 | 移除 `parkour-lane` DOM |
| c05-08 | 跑酷仍见紫条 | 禁用 `.code-theater-stage[data-theater-genre=parkour]::after` 网格 |
| c05-08 | 肉鸽粒子不可见 | WAAPI 勿 animate left/top · 用 transform 像素路径 · 提频 5–8/波 |

**经验**：

1. **品类专属禁用装饰**用 `data-theater-genre`，勿只删 overlay 子模块——stage 伪元素也会染色。
2. **粒子/飞行**优先 `transform` + `getBoundingClientRect()`，避免 WAAPI 动画 left/top 静默失败。
3. **build-wait** 与 **code overlay** 分属右栏/左栏，验收时需分别目视。

### 5.9 C-6 · 证书趣味标题 + PNG 保存（c06-01）

| 项 | 做法 |
|----|------|
| 标题 | `formatTitle(creator, displayName)` · spec `title_template` / `title_fallback` · `edu-wizard` 传 `creatorName` |
| 保存 | `#btnCertSave` · **vendor** `html2canvas` · 上传 PNG · 二维码 · 非 CDN |
| 弃用 | 主路径不再 `window.print` · `btn_print` 别名 `btn_save` |
| 局部 accent | `#edu-certificate` 设 `--cert-accent` / `--cert-accent-light` · 来自 `EduGenreTheme.themeFor` · 全页仍蓝白 |
| 双钮 | `.edu-cert-btn` min-height 56px（竖屏 64px）· 字号 ≥1.15rem |
| 精美化 | 双层边框 `.edu-certificate-frame` · 撒花 `.edu-cert-confetti` · 奖章 halo/core · 配方卡渐变 |
| E2E | 补 B2 创作者步 `#creatorInput` · 断言 `#btnCertSave` 与趣味标题 |

**经验**：证书叠层按钮 id 从 `btnCertPrint` 改为 `btnCertSave` 时须同步 E2E；html2canvas 已 vendor 本地化。

### 5.10 C-6 · 展馆专用服务器扫码（c06-04）

| 项 | 做法 |
|----|------|
| 公网 API | `GET /public/certificates/{token}` · token 72h · 不暴露 session_id |
| 上传 | `PUT /sessions/{id}/certificate` → `download_url` |
| 部署契约 | `GET /bootstrap` · `GET /health` → `certificate` 块 |
| 配置真相 | `backend/.env` `PUBLIC_API_BASE` + `config/exhibition_server.json` |
| 一键同步 | `05-工具脚本/apply_exhibition_config.ps1` |
| Nginx | `nginx_exhibition.conf.example` 须反代 `/public/certificates/` |
| 前端同步 | `session.js` `applyCertificateDeploy` 读 bootstrap |

### 5.11 C-6 · B2 保存名字 404 + session 自愈（c06-05）

| 项 | 做法 |
|----|------|
| 现象 | B2 点下一步 · `保存名字失败 - 404 Session not found` |
| 根因 | 后端重启丢 session · bootstrap 失败进 `demo-` · LAN 页访问但 `api_base=127.0.0.1` |
| 修复 | `EduSession.ensureSession()` 写前 GET 校验 · 404/demo 则 `POST /sessions` 重建 |
| LAN | `resolveApiBase()` 页面 host 非 localhost 时 API 跟同源 IP:8000 |
| 接入 | `edu-wizard.js` B2 子屏 A/B 写操作前 `await ensureSession()` |
| 文案 | 连不上后端 →「无法连接创作工坊，请检查网络或联系老师」 |

**经验**：

1. **凡写 session 的步骤**（B2 PATCH · wizard S0 · 证书上传）前应 `ensureSession()`，勿假设 bootstrap 后 session 永存。
2. **展馆多终端**访问时禁止写死 `127.0.0.1`；`kiosk_edu_spec.api_base` 可被 `resolveApiBase` 覆写为页面主机。
3. bootstrap 失败仍允许 UI 演示，但**写操作必须自愈或明确阻断**，避免 404 误报为「输入异常」。

### 5.12 C-7 · 日榜排行榜（2026-07-06 · 用户验收通过 · c07-10）

| 项 | 做法 |
|----|------|
| 存储 | `backend/data/leaderboard_daily/{genre}/{YYYY-MM-DD}.json` · 文件锁 + 原子写 |
| 关窗弹榜 | `edu-wizard.js` `pollLaunchStatus` · `sawGodotRunning && !running` · **沿边沿一次** |
| 轮询坑 | 勿依赖 `#godotRunStatus` 存在 · `startLaunchStatusPolling` 须在 `setRightContent` **之后** |
| 提交 | 关窗后 450ms 再读 `play/actions` 末条 `run_complete` |
| UI | `leaderboard.js` + `.edu-lb-*` · 七款 Tab · `EduGenreTheme` accent · 右侧三行成绩 |
| hooks | 仅 `templates/_edu/*_hooks.gd` · **workspace 须 B5 重生** 才注入 |
| 横版 | `level_reached` 优先 · 跨关 `elapsed_ms` 会话累计 · 胜利不关 session |
| 乒乓球 | GameOver **只缓存** · `session_end` 在关窗/回菜单 flush · 前端无 `session_end` 不 POST |

**热修表（c07-02~c07-10）**

| 版本 | 问题 | 修复 |
|------|------|------|
| c07-02 | 关窗不弹榜 | 轮询解耦 DOM · `sawGodotRunning` · 入口按钮 |
| c07-05~06 | 视觉弱/字小 | 粒子+光晕+字号+奖牌 |
| c07-07 | 右侧空 | `edu-lb-stats` 主/次/ tertiary 右对齐 |
| c07-08 | 横版仅分数 | `level_reached` 字段与排序 |
| c07-09 | 赛车/跑酷 tie 反了 | `elapsed_ms`/`survival_ms` **升序**作次键 |
| c07-09 | 生存/格斗 tie | 等级 secondary · 胜2平1负0 |
| c07-10 | 乒乓单分像上榜 | 延迟 `run_complete` · `session_end` 门闩 |

**经验**：

1. **排行榜弹层**与 **run_complete 写入**解耦：前者关窗触发，后者可缓存到退出（乒乓球）。
2. **sort_key 次字段**带时间时默认「越短越好」→ 用**正序** `elapsed_ms`，勿 `-elapsed_ms`。
3. 改 `leaderboard.js` / `edu-wizard.js` / CSS 须 bump `index.html` `?v=`（当前 `7.4-c07-10`）。

### 5.13 C-7 附 · B 链触控键盘热修（2026-07-06 · 用户验收通过 · c07-14）

| 项 | 做法 |
|----|------|
| 显 | `pointerdown` 输入 → `focus` + `show`（`AbortController` 取消未完成 hide） |
| 隐 | 点外/泡泡/导航 `pointerdown` → `blur` + `hide` · `bindNavDismiss` on `#btnNext`/`#btnPrev` |
| 后端 | `tabtip_native.py` COM Toggle · `_tabtip_open`（show 成功=true · hide 时必 Toggle） |
| 禁止 | `touchstart`+`pointerdown` 双触发 · `scheduleEnsure` 延迟补打 · `renderStep await dismiss` |
| 禁止 | `is_tabtip_visible()` 为假时 skip hide（键盘已开但 HWND 漏检） |
| 启动 | `launch_workshop.py` 或 8080+8000 双服务 · 见 §4.4 |

**热修表（c07-11~c07-14）**

| 版本 | 问题 | 修复 |
|------|------|------|
| c07-11 | 切页后输入中被关 | 导航 pointerdown dismiss · 去掉 renderStep await |
| c07-12 | 新页 2–3 次才出键盘 | 去掉冷却占 show · 去掉 ensure 定时器 |
| c07-13 | 仍有 API 延迟/闪烁 | native COM · warm · 意图状态机 |
| c07-14 | hide 不生效 | `_tabtip_open` · COM 单 worker 线程 · 导航钮 hide |

**足迹**：[`7.4_P4-C_触控键盘热修_施工足迹与验收总结_v1.0.md`](./7.4_P4-C_触控键盘热修_施工足迹与验收总结_v1.0.md)

### 5.14 C-8 · P4-C E2E 收工（2026-07-06）

| 项 | 做法 |
|----|------|
| 证书 E2E | B1 泡泡 + B2 创作者/游戏名双步 + 揭幕等待 + B4 逐卡确认 |
| 批量 E2E | `cleanup_sessions` 强校验 · 每 slug 后 `DELETE` 防 pool full |
| 主题 | 勿恢复 `body[data-genre-theme]` 整页换肤 · 局部 `themeFor()` 已够用 |
| 并行坑 | 证书 E2E 与 batch **勿并行**跑 · 共享 session 池 max 10 |

**经验**：UI 迭代后 E2E 须跟 B 链子屏（`b2SubStep` · `b4-card-flow` confirmedIndices），勿一次性 JS 填完全部题。

---

*v1.14 · 2026-07-06 · C-8 E2E 收工 · 证书+batch 修脚*
