# 横版格斗 · 冻结快照 v1.0

> **冻结时间**：2026-06-24  
> **状态**：✅ B7 用户验收通过（轻拳 · 重拳 · 格挡 · 大招 · 触控键）  
> **清单**：`config/frozen_genre_fighting.json`（SHA256）  
> **物理副本**：`开发文档/模板引擎/快照/frozen/6.24_fighting_v1.0/`

---

## 一、为何冻结

| 背景 | 说明 |
|------|------|
| B7 操作联动 | `light_punch` / `heavy_punch` / `block` / `special` → 代码高亮 + 黄字解说 |
| 展厅触控 | 专用虚拟键映射 `p1_*` 输入，避免误用其它品类按钮布局 |
| 风险 | 全品类 `edu-wizard` / `code-highlight` 热修曾误伤；格斗单独冻结防回退 |

---

## 二、冻结范围（硬冻结）

### 2.1 模板原件

| 品类 | slug | 路径 |
|------|------|------|
| 横版格斗 | `fighting` | `templates/fighting/**` |

含 `config/`、`core/`、`scenes/`、`project.godot` 及素材 `.import`。

### 2.2 B7 教育钩子与触控

| 文件 | 作用 |
|------|------|
| `templates/_edu/fighting_hooks.gd` | 上报四动作 action_id |
| `templates/_edu/fighting_touch_overlay.gd` | 对战时 P1 触控层 |

### 2.3 创作链配置

| 文件 | 作用 |
|------|------|
| `config/code_anchors/fighting.json` | B7 锚点 |
| `config/creative_templates/fighting.json` | B4 完型填空（无 skill_pick） |

---

## 三、B7 动作与优先级

| action_id | 锚点文件 | 行号 | 优先级 | 说明 |
|-----------|----------|------|--------|------|
| `light_punch` | `core/fighter.gd` | 290 | **9** | J 轻拳 |
| `heavy_punch` | `core/fighter.gd` | 294 | **9** | K 重拳 |
| `block` | `core/fighter.gd` | 283 | **9** | L 格挡 |
| `special` | `core/fighter.gd` | 298 | **9** | U 大招 |

---

## 四、关联冻结区（共享文件 · 慎改）

| 文件 | 与格斗的关系 |
|------|--------------|
| `templates/_edu/edu_action_bridge.gd` | 格斗动作去抖 |
| `kiosk/edu/code-highlight.js` | 动作优先级 |
| `config/kiosk_edu_spec.json` | `code_highlight` 段 |
| `kiosk/edu/edu-wizard.js` | `getGenreHighlightFallback().fighting` · B7 演示钮 |
| `backend/app/services/edu_workspace.py` | B5 复制触控脚本 · `FightingTouch` 节点 |

`edu-wizard.js` / `edu_workspace.py` 为全品类共用：**只允许改非 fighting 分支**。

---

## 五、验收基线

1. B5 重跑 → B6 启动 Godot → B7 硬刷新
2. 点「轻拳」「重拳」「格挡」「大招」→ `fighter.gd` 高亮 + 黄字
3. Godot 键盘 J/K/L/U 与触控虚拟键均触发 B7
4. 对战界面触控键位于画面下方左右分区，与格斗操作一致

---

## 六、工具链

```powershell
python 05-工具脚本\freeze_fighting_snapshot.py write
python 05-工具脚本\freeze_fighting_snapshot.py verify
```

发版前建议：

```powershell
node --check kiosk/edu/edu-wizard.js
python -m pytest backend/tests/test_edu_genre_hooks.py -q
```

---

## 七、Cursor 约束

[`.cursor/rules/frozen-fighting.mdc`](../../../.cursor/rules/frozen-fighting.mdc)

---

## 八、解冻记录

| 日期 | 范围 | 原因 | 操作人 | 新 manifest |
|------|------|------|--------|-------------|
| — | — | — | — | — |

---

## 九、关联文档

- [`6.24_横版格斗B7_进度快照_v1.0.md`](./6.24_横版格斗B7_进度快照_v1.0.md)
- [`6.24_横版闯关_冻结快照_v1.0.md`](./6.24_横版闯关_冻结快照_v1.0.md)
- [`工作状态记录_v1.0.md`](../工作状态记录_v1.0.md) §八
