# 赛车 · 冻结快照 v1.0

> **冻结时间**：2026-06-24  
> **状态**：✅ B7 用户验收通过（撞 NPC · 撞路障 · 转向最低优先级）  
> **清单**：`config/frozen_genre_racing.json`（SHA256）  
> **物理副本**：`开发文档/模板引擎/快照/frozen/6.24_racing_v1.0/`

---

## 一、为何冻结

| 背景 | 说明 |
|------|------|
| B7 碰撞 | `hit_npc` / `hit_trap` → `car_topdown.gd` 高亮 + 黄字解说 |
| 转向 | `steer` 保留展示，priority **1**（最低，碰撞时让位） |
| 风险 | 全品类 `code-highlight` 热修曾误伤其它游戏；赛车单独冻结防回退 |

---

## 二、冻结范围（硬冻结）

### 2.1 模板原件

| 品类 | slug | 路径 |
|------|------|------|
| 欢乐赛车 | `racing` | `templates/racing/**` |

含 `config/`、`core/`、`scenes/`、`project.godot` 及素材 `.import`。

### 2.2 B7 教育钩子

| 文件 | 上报 action_id |
|------|----------------|
| `templates/_edu/racing_hooks.gd` | `hit_npc` · `hit_trap` · `lap_complete` · `steer` |

### 2.3 创作链配置

| 文件 | 作用 |
|------|------|
| `config/code_anchors/racing.json` | B7 锚点 · 碰撞/转向/完圈 |
| `config/creative_templates/racing.json` | B4 完型填空 |

---

## 三、B7 动作与优先级

| action_id | 锚点文件 | 行号 | 优先级 | 说明 |
|-----------|----------|------|--------|------|
| `hit_npc` | `core/car_topdown.gd` | 149 | **9** | 撞 NPC |
| `hit_trap` | `core/car_topdown.gd` | 151 | **9** | 撞路障 |
| `lap_complete` | `core/track_runner.gd` | 142 | **8** | 完圈 |
| `steer` | `core/car_topdown.gd` | 126 | **1** | 左右转向（最低） |
| `boost` | — | — | — | **suppress**（不占线） |

---

## 四、关联冻结区（共享文件 · 慎改）

| 文件 | 与赛车的关系 |
|------|--------------|
| `templates/_edu/edu_action_bridge.gd` | `hit_npc`/`hit_trap`/`steer` 去抖 |
| `kiosk/edu/code-highlight.js` | 优先级 · `suppress: boost` |
| `config/kiosk_edu_spec.json` | `code_highlight` 段 |
| `kiosk/edu/edu-wizard.js` | `getGenreHighlightFallback().racing` · B7 演示钮 |

`edu-wizard.js` 为全品类共用：**只允许改非 racing 分支**；racing 保底锚点不得删改。

---

## 五、验收基线

1. B5 重跑 → B6 启动 Godot → B7
2. 点「撞车」「撞路障」→ `car_topdown.gd` 高亮 + 黄字
3. Godot 内真机碰撞同上
4. 空闲时按左右键 → `steer` 高亮（无碰撞时不被抢占）

---

## 六、工具链

```powershell
# 写入/更新冻结快照
python 05-工具脚本\freeze_racing_snapshot.py write

# 校验是否被意外修改
python 05-工具脚本\freeze_racing_snapshot.py verify
```

发版前建议同跑：

```powershell
node --check kiosk/edu/code-highlight.js
python -m pytest backend/tests/test_edu_genre_hooks.py -q
```

---

## 七、Cursor 约束

[`.cursor/rules/frozen-racing.mdc`](../../../.cursor/rules/frozen-racing.mdc)

---

## 八、解冻记录

| 日期 | 范围 | 原因 | 操作人 | 新 manifest |
|------|------|------|--------|-------------|
| — | — | — | — | — |

---

## 九、关联文档

- [`6.24_赛车B7_进度快照_v1.0.md`](./6.24_赛车B7_进度快照_v1.0.md)
- [`6.24_乒乓球与飞机_冻结快照_v1.0.md`](./6.24_乒乓球与飞机_冻结快照_v1.0.md)
- [`6.24_跑酷_冻结快照_v1.0.md`](./6.24_跑酷_冻结快照_v1.0.md)
- [`工作状态记录_v1.0.md`](../工作状态记录_v1.0.md) §八
