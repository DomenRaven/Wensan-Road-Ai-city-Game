# 7.20 · 可用性热修 · 榜单 / Diff 标注 / 无敌探针

> **日期**：2026-07-20  
> **品类探针**：shmup（登录 workspace `…/ec9bfaeb-…` · turns `trn_877c…` / `trn_d88e…` / `trn_15b6…`）  
> **已确认**：会话 404 误重建（V-1）验收通过，本单不重复  
> **Agent 续作**：半成品接线 → [`HF-14 证据验收`](./7.20_AI改游戏智能体_证据验收闭环_施工规范_v1.0.md)（✅）· UI 假绿/误杀 → [`HF-15 预言机分层`](./7.20_AI改游戏智能体_预言机分层_开工提示词_v1.0.md)

---

## 1. 缺陷与诊断

| ID | 现象 | 诊断 | 修复口径 |
|----|------|------|----------|
| **LB-2** | 首轮关窗不弹榜；与 AI 对话后才弹，且分数是首轮；此后每次 AI 对话完/重开改过的游戏也弹 | ① `edu-nlpatch-closed` 用 `pending \|\| sessionId` **兜底**；② `pending` 囤积后放出首轮分；③ launch 后 `sawGodotRunning` 仅认 `already_running` | 仅首轮未改局关窗弹榜；AI 开着只记 pending（关弹层禁 sessionId 兜底）；nl-patch 成功关闭自动弹榜；launch 认 `pid` |
| **DF-1** | 改动文件左侧绿条太细 | `.is-changed` 仅 `inset 3px` | 加宽竖条 + 浅绿底 + 圆点 + 「改」徽章 |
| **DF-2** | 新增代码绿底呈斑马线 | hunk 内 context 未标绿，夹在 `+` 之间 | context → `touch` 浅绿 |
| **AG-1** | 「每过 20 秒无敌」多轮「没生效」才可用；体感像击败充能 | **纠偏**：LLM 语义正确（始终时间轴表述）。T1 写了变量/HUD/效果检查，**未**写 `_update_god_mode` 调用；T2 声称已每帧更新但 Diff 无；T3 才接线。门禁见写入即过。「击败充能」多为 HUD「充能 %」+ 条不走的错觉，**非**套击杀模板 | **前端 tip 热修不够**。续作 HF-14：done 须 `evidence[]`，机器核「定义+接到 `_process`」；反馈轮无真 diff 不得复读 summary |

---

## 2. 探针摘要（终局磁盘 + 分轮 Diff）

终局 `player_ship.gd`：时间冷却 20s → 无敌 5s；`_process` → `_update_god_mode(delta)`。

分轮（learning_analytics blobs）：

- **T1** `trn_877c03d2784e4e1c`：vars / 射击与受伤检查 `_god_mode_active` / HUD；**无** `_update_god_mode`  
- **T2** `trn_d88ec3daef764971`：仅 `_ready` 初始化；summary 虚称已加每帧更新  
- **T3** `trn_15b689d6cc7d4321`：补上 `_update_god_mode` + `_process` 调用 → 生效  

---

## 3. 代码改动（本可用性窗）

| 项 | 文件 | 状态 |
|----|------|------|
| LB-2 | `kiosk/edu/edu-wizard.js` · `nl-patch-dialog.js` | ✅ |
| DF-1/2 | `code-browser.js` · `edu-styles.css` | ✅ |
| AG-1 tip（过渡→正式） | `game_agent.py` 时间/没生效 tip → **wired_by / evidence** | ✅ HF-14 |

---

## 4. 自测

1. 新会话 shmup → 首轮关窗 → 应自动榜；AI 改后关窗 / 关弹层 → 不自动榜。  
2. 改后全文：改动文件标记显眼；绿底非斑马。  
3. Agent：见 HF-14 DoD（半成品无敌不得过门）。

硬刷新（Ctrl+F5）验前端。

---

## 5. 进度

| 项 | 状态 |
|----|------|
| LB-2 / DF-1 / DF-2 | ✅ |
| AG-1 诊断纠偏 | ✅ |
| AG-1 根治（证据验收） | ✅ HF-14 S1–S5 |
| 表现假绿 / L1 误杀（8c76fabe） | ✅ HF-15 P0（合成单测） |
| V-1 会话误重建 | ✅ 已验收 |
