# 项目瘦身记录 · v1.2 展厅交付

> 日期：2026-07-17

## 已归档（`05-工具脚本/_dev_archive/` · 约 40 个）

- 秒哒 / 爬虫 / RAG 调研脚本（`miaoda_*`、`*_crawler*`、`build_rag_index` 等）
- 品类冻结与触控 verify 一次性脚本（`freeze_*`、`verify_*`）
- 过时 E2E 单测脚本（保留 batch / certificate / p3 三件冒烟）
- 素材导入、音效接线、方案 docx 渲染等一次性工具
- PyInstaller 乱码 `.spec`、`_batch_last.json`

## 已删除缓存 / 运行时

- `05-工具脚本/build/`、`dist/`
- `.pytest_cache/`、`.clawhub/`
- `workspace/` 会话内容、`backend/data/` 日榜运行时
- 根目录医疗导诊验收 docx（仅格式参考，非本产品）

## 交付压缩包刻意排除

| 排除项 | 原因 |
|--------|------|
| `.git` | 体积大 · 交付不需要 |
| `秒哒游戏原型/` | ~880MB 原型对照，运行不需要 |
| `03-背景与调研/` | 调研与 RAG 库 |
| `backend/.venv` | 目标机本地创建 |
| `05-工具脚本/_dev_archive` | 开发归档 |
| `开发文档/**/frozen` | 历史冻结快照 |
| `.cursor` | IDE 配置 |

## 保留的运维脚本

`deploy_exhibition.ps1` · `launch_workshop.py` · `build_launcher.ps1` · `run_backend.ps1` · `install_redis.ps1` · `run_redis.ps1` · `apply_exhibition_config.ps1` · `kiosk_lan_nginx.ps1` · 触控键盘辅助 ps1 · `validate_creative_templates.py` · 三件 E2E 冒烟 · `pack_exhibition_delivery.ps1`
